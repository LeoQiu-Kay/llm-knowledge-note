---
slug: agent-reliability-recovery
title: Agent 可靠性与故障恢复
---
# Agent 可靠性与故障恢复

> **先记住**：Agent 可靠性依赖可分类错误、有限重试、幂等副作用、超时与取消传播、持久化检查点和明确降级，而不是提示模型永不失败。

---

## 1. 它在解决什么问题？

Agent 可靠性依赖可分类错误、有限重试、幂等副作用、超时与取消传播、持久化检查点和明确降级，而不是提示模型永不失败。系统应区分模型、工具、编排、数据和策略故障，恢复时从最小安全步骤继续，并用故障注入验证真实行为。

读这一节时，先带着三个问题：

1. **核心对象是什么？** 哪些状态会在处理过程中发生变化？
2. **主链路怎么走？** 一次处理从哪里开始，到哪里才算结束？
3. **边界在哪里？** 数据量、并发或故障出现后，哪一环最先承压？

---

## 2. 先看全貌

![恢复决策依赖端到端执行证据](./assets/otel_architecture.png)

<p class="diagram-caption">先建立整体位置感：步骤写入意图与幂等键 → 执行外部副作用 → 保存结果或不确定状态 → 继续、补偿或升级人工</p>

### 主链路

```text
[步骤写入意图与幂等键]
    │
    ▼
[执行外部副作用]
    │
    ▼
[保存结果或不确定状态]
    │
    ▼
[重试前先对账外部系统]
    │
    ▼
[继续、补偿或升级人工]
```

先把这条链路记住即可。接下来再逐层拆开每一步为什么这样设计。

---

## 3. 核心机制，逐层拆开

### 错误分类

超时、限流和瞬时网络错误可有限重试；认证失败、schema 错误和业务拒绝应修复或终止；内容安全拦截需要安全降级。统一错误码要保留原始上下文但避免泄露敏感信息。

### 幂等与不确定结果

写工具携带业务幂等键，服务端记录请求结果。若调用超时但可能已成功，先查询外部状态再重试，不能把客户端未收到响应等同于未执行。

### 降级与恢复

可切换备用模型、只读模式、确定性工作流或人工处理。取消信号向子任务和工具传播，恢复扫描处理过期租约，死信队列保留无法自动解决的任务。

---

## 4. 用一段实现把概念落地

下面只保留最关键的主干。阅读时，把代码与上一节的流程一一对应：

```python
intent = store.begin_step(run_id, step_id, idempotency_key)
try:
    result = external_api.call(payload, idempotency_key=idempotency_key)
    store.complete_step(intent.id, result)
except TimeoutError:
    remote = external_api.lookup_by_idempotency_key(idempotency_key)
    if remote.succeeded:
        store.complete_step(intent.id, remote.result)
    else:
        scheduler.retry(intent.id, backoff="exponential+jitter", max_attempts=3)
```

### 对照着看

- **从哪里开始**：步骤写入意图与幂等键
- **关键状态变化**：保存结果或不确定状态
- **怎样才算完成**：继续、补偿或升级人工

---

## 5. 怎么选才不容易错

| 关注点 | 怎么理解 | 怎么验证 |
|---|---|---|
| **协议与边界** | 模型只提出意图，执行器和策略层掌握权限 | Schema、超时、幂等和审计在协议边界统一 |
| **确定性** | 关键合规节点由工作流控制 | Agent 只在受限工具集和预算内选择 |
| **恢复** | 副作用前写意图，完成后写结果 | 检查点与外部幂等键共同支持接管 |
| **评测** | 离线回归、线上观测和人工抽检闭环 | 版本化数据集并按失败类型切片 |

没有脱离场景的“最佳方案”。先写清数据规模、读写比例、故障模型和延迟目标，再做选择。

---

## 6. 放进真实系统，还要考虑什么

- 备用模型能力不同，切换前要验证工具调用、上下文长度和安全策略兼容性。
- 重试提高成功率也会放大下游压力，应使用抖动退避和全链路重试预算。
- 降级答案必须明确能力受限，不能静默返回看似完整的错误结果。

### 上线前检查

- 工具、提示、工作流、模型策略和评测集全部版本化。
- 副作用前写意图与幂等键，节点完成后原子保存检查点。
- 每个 run 保留可关联 Trace，但日志不记录密钥和私有推理。
- 发布门禁同时看任务成功、安全违规、成本和尾延迟。

---

## 7. 常见误区与排查

### 容易踩的坑

1. 模型层、SDK 和网关各自重试，形成指数级请求风暴。
2. 取消主任务后子 Agent 和浏览器仍继续运行并产生费用。
3. 工具超时后重复创建相同资源，没有幂等或对账。

### 出现问题时，按这个顺序看

1. **还原现场**：确认受影响的请求、数据、时间窗，以及最近是否有发布或流量变化。
2. **沿链路定位**：从“步骤写入意图与幂等键”一路检查到“继续、补偿或升级人工”，找出状态第一次偏离预期的位置。
3. **验证修复**：用最小复现、压力测试或故障注入证明问题消失，同时保留回滚方案。

---

## 8. 最后做一次复盘

> **一句话**：Agent 可靠性依赖可分类错误、有限重试、幂等副作用、超时与取消传播、持久化检查点和明确降级，而不是提示模型永不失败。
>
> **主链路**：步骤写入意图与幂等键 → 执行外部副作用 → 保存结果或不确定状态 → 重试前先对账外部系统 → 继续、补偿或升级人工
>
> **关键状态**：保存结果或不确定状态
>
> **最容易踩的坑**：模型层、SDK 和网关各自重试，形成指数级请求风暴。

---

## 9. 高频追问

### Q1. 请用一分钟说明Agent 可靠性与故障恢复的核心目标与工作机制。

Agent 可靠性依赖可分类错误、有限重试、幂等副作用、超时与取消传播、持久化检查点和明确降级，而不是提示模型永不失败。系统应区分模型、工具、编排、数据和策略故障，恢复时从最小安全步骤继续，并用故障注入验证真实行为。

### Q2. 错误分类的核心机制是什么？

超时、限流和瞬时网络错误可有限重试；认证失败、schema 错误和业务拒绝应修复或终止；内容安全拦截需要安全降级。统一错误码要保留原始上下文但避免泄露敏感信息。

### Q3. 幂等与不确定结果为什么重要，实际如何落地？

写工具携带业务幂等键，服务端记录请求结果。若调用超时但可能已成功，先查询外部状态再重试，不能把客户端未收到响应等同于未执行。

### Q4. Agent 可靠性与故障恢复在工程落地时如何做取舍？

备用模型能力不同，切换前要验证工具调用、上下文长度和安全策略兼容性；重试提高成功率也会放大下游压力，应使用抖动退避和全链路重试预算；降级答案必须明确能力受限，不能静默返回看似完整的错误结果。

### Q5. Agent 可靠性与故障恢复最常见的故障与排查重点是什么？

模型层、SDK 和网关各自重试，形成指数级请求风暴；取消主任务后子 Agent 和浏览器仍继续运行并产生费用；工具超时后重复创建相同资源，没有幂等或对账。排查时应关联运行状态、模型请求、工具调用、外部证据和最终结果，先判断是数据、模型、编排、权限还是依赖故障。
