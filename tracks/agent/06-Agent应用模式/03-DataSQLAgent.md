---
slug: agent-data-sql
title: Data/SQL Agent
---
# Data/SQL Agent

> **先记住**：Data/SQL Agent 把自然语言目标转换为数据发现、查询、分析和解释。

---

## 1. 它在解决什么问题？

Data/SQL Agent 把自然语言目标转换为数据发现、查询、分析和解释。安全实现先获取 schema 与业务口径，生成只读、受限查询，经解析器和策略校验后在隔离账户执行，再验证结果与引用；写操作、敏感字段和大规模扫描必须走更严格审批或专用工作流。

读这一节时，先带着三个问题：

1. **核心对象是什么？** 哪些状态会在处理过程中发生变化？
2. **主链路怎么走？** 一次处理从哪里开始，到哪里才算结束？
3. **边界在哪里？** 数据量、并发或故障出现后，哪一环最先承压？

---

## 2. 先看全貌

![Schema 与证据检索约束 SQL 生成](./assets/rag_diagram.png)

<p class="diagram-caption">先建立整体位置感：解析业务问题与指标口径 → 检索 schema 和样例 → 生成只读 SQL → 执行、解释结果并保留查询证据</p>

### 主链路

```text
[解析业务问题与指标口径]
    │
    ▼
[检索 schema 和样例]
    │
    ▼
[生成只读 SQL]
    │
    ▼
[静态校验并限制扫描量]
    │
    ▼
[执行、解释结果并保留查询证据]
```

先把这条链路记住即可。接下来再逐层拆开每一步为什么这样设计。

---

## 3. 核心机制，逐层拆开

### 语义与 schema

向模型提供表、字段、主外键、指标定义、时间口径和样例值，而不是完整数据。语义层统一“活跃用户”等业务指标，避免每次让模型猜 join 和过滤条件。

### 查询护栏

使用只读凭证、允许列表、AST 解析和参数化查询，限制表、列、行数、扫描量、执行时间和并发。先 EXPLAIN 评估成本，禁止多语句、DDL/DML 和不受控函数。

### 结果验证

检查空值、重复、时间范围、单位和总分关系，对关键结论运行交叉查询或规则校验。答案展示 SQL、数据时间和口径，区分观察结果与因果推断。

---

## 4. 用一段实现把概念落地

下面只保留最关键的主干。阅读时，把代码与上一节的流程一一对应：

```sql
-- 生成后先静态校验：只读、单语句、强制 tenant 条件和行数上限。
EXPLAIN
SELECT date_trunc('day', paid_at) AS day,
       COUNT(*) AS orders,
       SUM(amount) AS revenue
  FROM orders
 WHERE tenant_id = :tenant_id
   AND paid_at >= :start_at
   AND paid_at < :end_at
 GROUP BY 1
 ORDER BY 1
 LIMIT 1000;
```

### 对照着看

- **从哪里开始**：解析业务问题与指标口径
- **关键状态变化**：生成只读 SQL
- **怎样才算完成**：执行、解释结果并保留查询证据

---

## 5. 怎么选才不容易错

| 关注点 | 怎么理解 | 怎么验证 |
|---|---|---|
| **可验证性** | 每一步都有可观察前置状态和验收条件 | DOM、测试、SQL 计划或引用充当证据 |
| **副作用** | 发送、购买、删除和提交必须明确确认 | 展示 diff、目标、风险和撤销方式 |
| **用户控制** | 计划、进度、暂停、取消和接管始终可见 | 失败时给可恢复下一步，不只报错 |
| **领域约束** | 通用模型不能绕过代码、数据、语音或搜索领域规则 | 专用校验器和最小权限工具守门 |

没有脱离场景的“最佳方案”。先写清数据规模、读写比例、故障模型和延迟目标，再做选择。

---

## 6. 放进真实系统，还要考虑什么

- 实时 schema 提高准确性，也可能泄露敏感表名，应按用户权限裁剪。
- 自动修复 SQL 提高成功率，但重试必须保持同一安全策略和预算。
- 把常用问题固化为指标 API 更可靠，长尾探索再使用 Agent。

### 上线前检查

- 对用户展示计划、进度、证据和即将发生的副作用。
- 观察—行动—再观察，每一步都验证真实状态变化。
- 领域校验器限制 SQL、代码、浏览器、语音和搜索的危险边界。
- 支持暂停、取消、重试、编辑、撤销和人工接管。

---

## 7. 常见误区与排查

### 容易踩的坑

1. 模型生成笛卡尔积，扫描量和费用失控。
2. 查询成功但使用错误时区或业务口径，结论错误。
3. 结果集包含个人信息并被完整写入模型日志。

### 出现问题时，按这个顺序看

1. **还原现场**：确认受影响的请求、数据、时间窗，以及最近是否有发布或流量变化。
2. **沿链路定位**：从“解析业务问题与指标口径”一路检查到“执行、解释结果并保留查询证据”，找出状态第一次偏离预期的位置。
3. **验证修复**：用最小复现、压力测试或故障注入证明问题消失，同时保留回滚方案。

---

## 8. 最后做一次复盘

> **一句话**：Data/SQL Agent 把自然语言目标转换为数据发现、查询、分析和解释。
>
> **主链路**：解析业务问题与指标口径 → 检索 schema 和样例 → 生成只读 SQL → 静态校验并限制扫描量 → 执行、解释结果并保留查询证据
>
> **关键状态**：生成只读 SQL
>
> **最容易踩的坑**：模型生成笛卡尔积，扫描量和费用失控。

---

## 9. 高频追问

### Q1. 请用一分钟说明Data/SQL Agent的核心目标与工作机制。

Data/SQL Agent 把自然语言目标转换为数据发现、查询、分析和解释。安全实现先获取 schema 与业务口径，生成只读、受限查询，经解析器和策略校验后在隔离账户执行，再验证结果与引用；写操作、敏感字段和大规模扫描必须走更严格审批或专用工作流。

### Q2. 语义与 schema的核心机制是什么？

向模型提供表、字段、主外键、指标定义、时间口径和样例值，而不是完整数据。语义层统一“活跃用户”等业务指标，避免每次让模型猜 join 和过滤条件。

### Q3. 查询护栏为什么重要，实际如何落地？

使用只读凭证、允许列表、AST 解析和参数化查询，限制表、列、行数、扫描量、执行时间和并发。先 EXPLAIN 评估成本，禁止多语句、DDL/DML 和不受控函数。

### Q4. Data/SQL Agent在工程落地时如何做取舍？

实时 schema 提高准确性，也可能泄露敏感表名，应按用户权限裁剪；自动修复 SQL 提高成功率，但重试必须保持同一安全策略和预算；把常用问题固化为指标 API 更可靠，长尾探索再使用 Agent。

### Q5. Data/SQL Agent最常见的故障与排查重点是什么？

模型生成笛卡尔积，扫描量和费用失控；查询成功但使用错误时区或业务口径，结论错误；结果集包含个人信息并被完整写入模型日志。排查时应关联运行状态、模型请求、工具调用、外部证据和最终结果，先判断是数据、模型、编排、权限还是依赖故障。
