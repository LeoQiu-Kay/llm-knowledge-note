---
slug: agent-search-deep-research
title: Agentic Search 与 Deep Research
---
# Agentic Search 与 Deep Research

> **先记住**：Agentic Search/Deep Research 将复杂问题拆解为检索子题，迭代搜索、阅读、交叉验证并合成带引用报告。

---

## 1. 它在解决什么问题？

Agentic Search/Deep Research 将复杂问题拆解为检索子题，迭代搜索、阅读、交叉验证并合成带引用报告。高质量系统要规划覆盖面、控制来源质量和时效、去重与发现矛盾，保存查询和证据链，并在预算内判断何时信息已经足够，而不是无限浏览。

读这一节时，先带着三个问题：

1. **核心对象是什么？** 哪些状态会在处理过程中发生变化？
2. **主链路怎么走？** 一次处理从哪里开始，到哪里才算结束？
3. **边界在哪里？** 数据量、并发或故障出现后，哪一环最先承压？

---

## 2. 先看全貌

![搜索证据进入模型前要检索与筛选](./assets/rag_diagram.png)

<p class="diagram-caption">先建立整体位置感：拆解研究问题 → 并行搜索多个独立来源 → 抽取证据与发布日期 → 带引用综合结论和不确定性</p>

### 主链路

```text
[拆解研究问题]
    │
    ▼
[并行搜索多个独立来源]
    │
    ▼
[抽取证据与发布日期]
    │
    ▼
[交叉验证冲突并补搜]
    │
    ▼
[带引用综合结论和不确定性]
```

先把这条链路记住即可。接下来再逐层拆开每一步为什么这样设计。

---

## 3. 核心机制，逐层拆开

### 研究计划

先澄清问题范围、时间和输出标准，再拆成可验证子问题与关键词。计划随发现更新，但要记录覆盖矩阵，防止只深挖一个方向而遗漏关键维度。

### 来源与证据

优先一手资料、官方文档和高信誉来源，记录发布日期、事件日期、作者和访问时间。重要事实至少交叉核对，区分直接证据、来源观点与系统推断。

### 综合与停止

对重复内容聚类，对冲突展示双方证据与时间，按主题生成结论并逐句引用。停止条件可基于子题覆盖、连续搜索无新信息、置信度和成本预算。

---

## 4. 用一段实现把概念落地

下面只保留最关键的主干。阅读时，把代码与上一节的流程一一对应：

```python
questions = planner.decompose(research_question)
sources = parallel_map(search, questions)
evidence = [
    extract(source, fields=["claim", "quote", "published_at", "author", "url"])
    for source in deduplicate(sources)
    if source_reliability(source) >= MIN_RELIABILITY
]
conflicts = detect_conflicting_claims(evidence)
evidence += targeted_follow_up_search(conflicts)
return synthesize(evidence, cite_every_claim=True, state_uncertainty=True)
```

### 对照着看

- **从哪里开始**：拆解研究问题
- **关键状态变化**：抽取证据与发布日期
- **怎样才算完成**：带引用综合结论和不确定性

---

## 5. 怎么选才不容易错

| 关注点 | 怎么理解 | 怎么验证 |
|---|---|---|
| **可验证性** | 每一步都有可观察前置状态和验收条件 | DOM、测试、SQL 计划或引用充当证据 |
| **副作用** | 发送、购买、删除和提交必须明确确认 | 展示 diff、目标、风险和撤销方式 |
| **用户控制** | 计划、进度、暂停、取消和接管始终可见 | 失败时给可恢复下一步，不只报错 |
| **领域约束** | 通用模型不能绕过代码、数据、语音或搜索领域规则 | 专用校验器和最小权限工具守门 |

没有脱离场景的“最佳方案”。先写清数据规模、读写比例、故障模型和延迟目标，再做选择。

---

## 6. 放进真实系统，还要考虑什么

- 搜索更广提高召回，却会增加低质量来源和综合成本。
- 只用最新来源可能忽略基础资料，时效性要按问题类型判断。
- 并行研究降低延迟，但需要统一术语、去重和证据合并。

### 上线前检查

- 对用户展示计划、进度、证据和即将发生的副作用。
- 观察—行动—再观察，每一步都验证真实状态变化。
- 领域校验器限制 SQL、代码、浏览器、语音和搜索的危险边界。
- 支持暂停、取消、重试、编辑、撤销和人工接管。

---

## 7. 常见误区与排查

### 容易踩的坑

1. 引用页面真实存在，但正文没有支持对应结论。
2. 把文章发布日期误当事件发生日期。
3. 搜索结果中的 SEO 聚合站相互引用，形成虚假多源验证。

### 出现问题时，按这个顺序看

1. **还原现场**：确认受影响的请求、数据、时间窗，以及最近是否有发布或流量变化。
2. **沿链路定位**：从“拆解研究问题”一路检查到“带引用综合结论和不确定性”，找出状态第一次偏离预期的位置。
3. **验证修复**：用最小复现、压力测试或故障注入证明问题消失，同时保留回滚方案。

---

## 8. 最后做一次复盘

> **一句话**：Agentic Search/Deep Research 将复杂问题拆解为检索子题，迭代搜索、阅读、交叉验证并合成带引用报告。
>
> **主链路**：拆解研究问题 → 并行搜索多个独立来源 → 抽取证据与发布日期 → 交叉验证冲突并补搜 → 带引用综合结论和不确定性
>
> **关键状态**：抽取证据与发布日期
>
> **最容易踩的坑**：引用页面真实存在，但正文没有支持对应结论。

---

## 9. 高频追问

### Q1. 请用一分钟说明Agentic Search 与 Deep Research的核心目标与工作机制。

Agentic Search/Deep Research 将复杂问题拆解为检索子题，迭代搜索、阅读、交叉验证并合成带引用报告。高质量系统要规划覆盖面、控制来源质量和时效、去重与发现矛盾，保存查询和证据链，并在预算内判断何时信息已经足够，而不是无限浏览。

### Q2. 研究计划的核心机制是什么？

先澄清问题范围、时间和输出标准，再拆成可验证子问题与关键词。计划随发现更新，但要记录覆盖矩阵，防止只深挖一个方向而遗漏关键维度。

### Q3. 来源与证据为什么重要，实际如何落地？

优先一手资料、官方文档和高信誉来源，记录发布日期、事件日期、作者和访问时间。重要事实至少交叉核对，区分直接证据、来源观点与系统推断。

### Q4. Agentic Search 与 Deep Research在工程落地时如何做取舍？

搜索更广提高召回，却会增加低质量来源和综合成本；只用最新来源可能忽略基础资料，时效性要按问题类型判断；并行研究降低延迟，但需要统一术语、去重和证据合并。

### Q5. Agentic Search 与 Deep Research最常见的故障与排查重点是什么？

引用页面真实存在，但正文没有支持对应结论；把文章发布日期误当事件发生日期；搜索结果中的 SEO 聚合站相互引用，形成虚假多源验证。排查时应关联运行状态、模型请求、工具调用、外部证据和最终结果，先判断是数据、模型、编排、权限还是依赖故障。
