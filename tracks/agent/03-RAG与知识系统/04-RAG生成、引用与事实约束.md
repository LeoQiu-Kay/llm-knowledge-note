---
slug: rag-grounded-generation-citation
title: RAG 生成、引用与事实约束
---
# RAG 生成、引用与事实约束

> **先记住**：RAG 生成阶段要把检索证据作为低信任数据提供给模型，要求基于证据回答、区分事实与推断，并生成可验证引用。

---

## 1. 它在解决什么问题？

RAG 生成阶段要把检索证据作为低信任数据提供给模型，要求基于证据回答、区分事实与推断，并生成可验证引用。系统还需检测证据不足、相互冲突和过期，必要时拒答或请求澄清；有引用不等于有依据，引用必须真正支持对应陈述。

读这一节时，先带着三个问题：

1. **核心对象是什么？** 哪些状态会在处理过程中发生变化？
2. **主链路怎么走？** 一次处理从哪里开始，到哪里才算结束？
3. **边界在哪里？** 数据量、并发或故障出现后，哪一环最先承压？

---

## 2. 先看全貌

![生成答案必须回到检索证据](./assets/rag_diagram.png)

<p class="diagram-caption">先建立整体位置感：检索可引用证据 → 按来源组织上下文 → 提示模型只基于证据回答 → 验证引用是否真的支持结论</p>

### 主链路

```text
[检索可引用证据]
    │
    ▼
[按来源组织上下文]
    │
    ▼
[提示模型只基于证据回答]
    │
    ▼
[句子级绑定引用]
    │
    ▼
[验证引用是否真的支持结论]
```

先把这条链路记住即可。接下来再逐层拆开每一步为什么这样设计。

---

## 3. 核心机制，逐层拆开

### 证据编排

对证据去重、按主题分组并附 source_id、标题、时间和定位。提示中明确外部内容不得覆盖系统规则，长文先选择相关段落而不是无差别塞入。

### 引用对齐

让模型按稳定 source_id 引用，并在生成后校验引用存在、权限有效、文本蕴含陈述。句级引用比答案末尾罗列链接更易审计，关键结论可使用 NLI 或二次验证。

### 不足与冲突

定义最低证据阈值和时效要求；找不到答案时返回缺失信息而非依赖模型参数记忆。多来源冲突要展示差异、优先权威/最新来源或升级人工判断。

---

## 4. 用一段实现把概念落地

下面只保留最关键的主干。阅读时，把代码与上一节的流程一一对应：

```python
evidence = retriever.search(question, acl=user.acl)
answer = model.generate(
    question=question,
    evidence=[{"id": e.id, "text": e.text} for e in evidence],
    rules="Only claim facts supported by evidence; cite evidence ids per sentence.",
)
for claim in answer.claims:
    support = citation_checker.verify(claim.text, claim.citation_ids, evidence)
    if not support.entails:
        claim.mark_unsupported()
return answer
```

### 对照着看

- **从哪里开始**：检索可引用证据
- **关键状态变化**：提示模型只基于证据回答
- **怎样才算完成**：验证引用是否真的支持结论

---

## 5. 怎么选才不容易错

| 关注点 | 怎么理解 | 怎么验证 |
|---|---|---|
| **召回** | 先保证正确证据能进入候选集 | 用 recall@k、MRR 和无答案集验证 |
| **排序** | 词法、向量与图检索各有盲区 | 按查询类型切片比较，不只看总分 |
| **生成** | 回答必须受证据约束并绑定引用 | 逐 claim 验证引用支持关系 |
| **新鲜度与权限** | 索引必须携带版本、时间和 ACL | 检索前过滤，删除与更新可追踪 |

没有脱离场景的“最佳方案”。先写清数据规模、读写比例、故障模型和延迟目标，再做选择。

---

## 6. 放进真实系统，还要考虑什么

- 严格只按证据回答可降低幻觉，但对开放建议类问题可能过度保守。
- 逐句验证质量更高，也增加模型调用和延迟。
- 引用原文片段有助核验，同时要控制敏感信息和展示长度。

### 上线前检查

- 文档、chunk、embedding、索引和提示版本可追踪。
- 权限在检索阶段过滤，不能只靠生成阶段提醒模型保密。
- 分别评估召回、排序、事实性、引用精度和无答案拒答。
- 建立删除、更新、反馈和错误证据回流的数据闭环。

---

## 7. 常见误区与排查

### 容易踩的坑

1. 模型引用真实文档，但该文档并不支持对应数字。
2. 把检索文本中的恶意指令当成系统命令执行。
3. 证据来自旧政策，回答却使用现在时且不标日期。

### 出现问题时，按这个顺序看

1. **还原现场**：确认受影响的请求、数据、时间窗，以及最近是否有发布或流量变化。
2. **沿链路定位**：从“检索可引用证据”一路检查到“验证引用是否真的支持结论”，找出状态第一次偏离预期的位置。
3. **验证修复**：用最小复现、压力测试或故障注入证明问题消失，同时保留回滚方案。

---

## 8. 最后做一次复盘

> **一句话**：RAG 生成阶段要把检索证据作为低信任数据提供给模型，要求基于证据回答、区分事实与推断，并生成可验证引用。
>
> **主链路**：检索可引用证据 → 按来源组织上下文 → 提示模型只基于证据回答 → 句子级绑定引用 → 验证引用是否真的支持结论
>
> **关键状态**：提示模型只基于证据回答
>
> **最容易踩的坑**：模型引用真实文档，但该文档并不支持对应数字。

---

## 9. 高频追问

### Q1. 请用一分钟说明RAG 生成、引用与事实约束的核心目标与工作机制。

RAG 生成阶段要把检索证据作为低信任数据提供给模型，要求基于证据回答、区分事实与推断，并生成可验证引用。系统还需检测证据不足、相互冲突和过期，必要时拒答或请求澄清；有引用不等于有依据，引用必须真正支持对应陈述。

### Q2. 证据编排的核心机制是什么？

对证据去重、按主题分组并附 source_id、标题、时间和定位。提示中明确外部内容不得覆盖系统规则，长文先选择相关段落而不是无差别塞入。

### Q3. 引用对齐为什么重要，实际如何落地？

让模型按稳定 source_id 引用，并在生成后校验引用存在、权限有效、文本蕴含陈述。句级引用比答案末尾罗列链接更易审计，关键结论可使用 NLI 或二次验证。

### Q4. RAG 生成、引用与事实约束在工程落地时如何做取舍？

严格只按证据回答可降低幻觉，但对开放建议类问题可能过度保守；逐句验证质量更高，也增加模型调用和延迟；引用原文片段有助核验，同时要控制敏感信息和展示长度。

### Q5. RAG 生成、引用与事实约束最常见的故障与排查重点是什么？

模型引用真实文档，但该文档并不支持对应数字；把检索文本中的恶意指令当成系统命令执行；证据来自旧政策，回答却使用现在时且不标日期。排查时应关联运行状态、模型请求、工具调用、外部证据和最终结果，先判断是数据、模型、编排、权限还是依赖故障。
