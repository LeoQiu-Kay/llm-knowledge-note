---
slug: rag-ingestion-chunking
title: 文档解析、切分与元数据
---
# 文档解析、切分与元数据

> **先记住**：RAG 的上限首先由摄取质量决定：需要正确解析正文、表格、图片和层级结构，按语义与检索用途切分，并为每个 chunk 保存文档、章节、版本、时间、权限和定位信息。

---

## 1. 它在解决什么问题？

RAG 的上限首先由摄取质量决定：需要正确解析正文、表格、图片和层级结构，按语义与检索用途切分，并为每个 chunk 保存文档、章节、版本、时间、权限和定位信息。切分不是固定字符数问题，而是召回完整性、精确度和生成上下文成本的权衡。

读这一节时，先带着三个问题：

1. **核心对象是什么？** 哪些状态会在处理过程中发生变化？
2. **主链路怎么走？** 一次处理从哪里开始，到哪里才算结束？
3. **边界在哪里？** 数据量、并发或故障出现后，哪一环最先承压？

---

## 2. 先看全貌

![离线索引决定在线能检索到什么](./assets/rag_diagram.png)

<p class="diagram-caption">先建立整体位置感：解析原始文档 → 保留标题、表格与页码结构 → 按语义和长度切分 → 嵌入并写入可版本化索引</p>

### 主链路

```text
[解析原始文档]
    │
    ▼
[保留标题、表格与页码结构]
    │
    ▼
[按语义和长度切分]
    │
    ▼
[补充来源、权限和时间元数据]
    │
    ▼
[嵌入并写入可版本化索引]
```

先把这条链路记住即可。接下来再逐层拆开每一步为什么这样设计。

---

## 3. 核心机制，逐层拆开

### 解析与规范化

针对 PDF、HTML、Office 和扫描件选择解析器，保留标题、列表、表格关系和页码；去除导航、页眉页脚与重复模板，同时保留原始文件哈希供追溯。

### 切分策略

优先按标题、段落、代码块和表格等语义边界切分，再设置最大长度与少量重叠。事实密集内容用小块，流程和论证需要更大上下文，可保存父子 chunk 以小块召回、大块生成。

### 元数据与版本

记录 source、document_id、section_path、updated_at、language、tenant、ACL 和 parser_version。更新时按稳定 ID 增量替换，删除源文档要同步清理索引。

---

## 4. 用一段实现把概念落地

下面只保留最关键的主干。阅读时，把代码与上一节的流程一一对应：

```python
document = parser.parse(blob, keep_pages=True, keep_tables=True)
for section in document.semantic_sections():
    for chunk in split(section, target_tokens=450, overlap_tokens=60):
        index.upsert(
            id=stable_chunk_id(document.version, chunk.path, chunk.offset),
            text=chunk.text,
            metadata={
                "source": document.uri, "page": chunk.page,
                "heading": chunk.path, "acl": document.acl,
                "valid_at": document.updated_at,
            },
        )
```

### 对照着看

- **从哪里开始**：解析原始文档
- **关键状态变化**：按语义和长度切分
- **怎样才算完成**：嵌入并写入可版本化索引

---

## 5. 怎么选才不容易错

| 关注点 | 怎么理解 | 怎么验证 |
|---|---|---|
| **召回** | 先保证正确证据能进入候选集 | 用 recall@k、MRR 和无答案集验证 |
| **排序** | 词法、向量与图检索各有盲区 | 按查询类型切片比较，不只看总分 |
| **生成** | 回答必须受证据约束并绑定引用 | 逐 claim 验证引用支持关系 |
| **新鲜度与权限** | 索引必须携带版本、时间和 ACL | 检索前过滤，删除与更新可追踪 |

没有脱离场景的“最佳方案”。先写清数据规模、读写比例、故障模型和延迟目标，再做选择。

---

## 6. 放进真实系统，还要考虑什么

- 重叠可缓解边界丢失，但会造成重复召回和索引膨胀。
- OCR 提高覆盖率，也可能引入数字、表格和专有名词错误，需要质量阈值。
- 通用切分简单，领域专用解析效果更好但维护成本更高。

### 上线前检查

- 文档、chunk、embedding、索引和提示版本可追踪。
- 权限在检索阶段过滤，不能只靠生成阶段提醒模型保密。
- 分别评估召回、排序、事实性、引用精度和无答案拒答。
- 建立删除、更新、反馈和错误证据回流的数据闭环。

---

## 7. 常见误区与排查

### 容易踩的坑

1. 表格被逐行打散，生成答案失去列关系。
2. 文档更新只新增新 chunk，旧版本仍被检索。
3. 索引未携带 ACL，检索后才过滤导致越权与召回不足。

### 出现问题时，按这个顺序看

1. **还原现场**：确认受影响的请求、数据、时间窗，以及最近是否有发布或流量变化。
2. **沿链路定位**：从“解析原始文档”一路检查到“嵌入并写入可版本化索引”，找出状态第一次偏离预期的位置。
3. **验证修复**：用最小复现、压力测试或故障注入证明问题消失，同时保留回滚方案。

---

## 8. 最后做一次复盘

> **一句话**：RAG 的上限首先由摄取质量决定：需要正确解析正文、表格、图片和层级结构，按语义与检索用途切分，并为每个 chunk 保存文档、章节、版本、时间、权限和定位信息。
>
> **主链路**：解析原始文档 → 保留标题、表格与页码结构 → 按语义和长度切分 → 补充来源、权限和时间元数据 → 嵌入并写入可版本化索引
>
> **关键状态**：按语义和长度切分
>
> **最容易踩的坑**：表格被逐行打散，生成答案失去列关系。

---

## 9. 高频追问

### Q1. 请用一分钟说明文档解析、切分与元数据的核心目标与工作机制。

RAG 的上限首先由摄取质量决定：需要正确解析正文、表格、图片和层级结构，按语义与检索用途切分，并为每个 chunk 保存文档、章节、版本、时间、权限和定位信息。切分不是固定字符数问题，而是召回完整性、精确度和生成上下文成本的权衡。

### Q2. 解析与规范化的核心机制是什么？

针对 PDF、HTML、Office 和扫描件选择解析器，保留标题、列表、表格关系和页码；去除导航、页眉页脚与重复模板，同时保留原始文件哈希供追溯。

### Q3. 切分策略为什么重要，实际如何落地？

优先按标题、段落、代码块和表格等语义边界切分，再设置最大长度与少量重叠。事实密集内容用小块，流程和论证需要更大上下文，可保存父子 chunk 以小块召回、大块生成。

### Q4. 文档解析、切分与元数据在工程落地时如何做取舍？

重叠可缓解边界丢失，但会造成重复召回和索引膨胀；OCR 提高覆盖率，也可能引入数字、表格和专有名词错误，需要质量阈值；通用切分简单，领域专用解析效果更好但维护成本更高。

### Q5. 文档解析、切分与元数据最常见的故障与排查重点是什么？

表格被逐行打散，生成答案失去列关系；文档更新只新增新 chunk，旧版本仍被检索；索引未携带 ACL，检索后才过滤导致越权与召回不足。排查时应关联运行状态、模型请求、工具调用、外部证据和最终结果，先判断是数据、模型、编排、权限还是依赖故障。
