---
slug: rag-hybrid-rerank-query
title: 混合检索、重排与查询改写
---
# 混合检索、重排与查询改写

> **先记住**：高质量检索通常把 BM25 等词法召回与向量召回融合，再用重排模型对较小候选集精排。

---

## 1. 它在解决什么问题？

高质量检索通常把 BM25 等词法召回与向量召回融合，再用重排模型对较小候选集精排。查询改写可补全缩写、拆分多跳问题或生成多个检索视角，但必须保留原意；完整链路应分别评估召回、重排和最终答案，不能只看一次相似度。

读这一节时，先带着三个问题：

1. **核心对象是什么？** 哪些状态会在处理过程中发生变化？
2. **主链路怎么走？** 一次处理从哪里开始，到哪里才算结束？
3. **边界在哪里？** 数据量、并发或故障出现后，哪一环最先承压？

---

## 2. 先看全貌

![多路召回后再把证据交给生成器](./assets/rag_diagram.png)

<p class="diagram-caption">先建立整体位置感：理解查询意图 → 生成词法与语义检索请求 → BM25 和向量召回并集 → 按证据覆盖选择上下文</p>

### 主链路

```text
[理解查询意图]
    │
    ▼
[生成词法与语义检索请求]
    │
    ▼
[BM25 和向量召回并集]
    │
    ▼
[RRF / Cross-Encoder 重排]
    │
    ▼
[按证据覆盖选择上下文]
```

先把这条链路记住即可。接下来再逐层拆开每一步为什么这样设计。

---

## 3. 核心机制，逐层拆开

### 混合召回

词法检索擅长专有名词、编号和精确短语，向量检索擅长语义改写。可用 Reciprocal Rank Fusion 等方法合并不同分数量纲的排名，并对字段和来源设权重。

### 重排

Cross-encoder 或生成式重排器联合读取 query 与候选，相关性通常更高但成本更大。先宽召回几十到几百条，再重排到生成所需少量证据，并设置超时降级。

### 查询改写

规范化实体、扩展同义词、将复杂问题拆成子查询，或用假设答案辅助召回。所有改写要与原查询一起保留，防止模型臆造条件后只检索错误方向。

---

## 4. 用一段实现把概念落地

下面只保留最关键的主干。阅读时，把代码与上一节的流程一一对应：

```python
dense = vector_index.search(embed(query), top_k=50)
sparse = bm25.search(query, top_k=50)

def rrf(rank, k=60):
    return 1.0 / (k + rank)

fused = reciprocal_rank_fusion(dense, sparse, score_fn=rrf)
rewritten = query_rewriter.add_required_entities(query, conversation)
return cross_encoder.rerank(rewritten, fused[:40])[:8]
```

### 对照着看

- **从哪里开始**：理解查询意图
- **关键状态变化**：BM25 和向量召回并集
- **怎样才算完成**：按证据覆盖选择上下文

---

## 5. 怎么选才不容易错

| 关注点 | 怎么理解 | 怎么验证 |
|---|---|---|
| **召回** | 先保证正确证据能进入候选集 | 用 recall@k、MRR 和无答案集验证 |
| **排序** | 词法、向量与图检索各有盲区 | 按查询类型切片比较，不只看总分 |
| **生成** | 回答必须受证据约束并绑定引用 | 逐 claim 验证引用支持关系 |
| **新鲜度与权限** | 索引必须携带版本、时间和 ACL | 检索前过滤，删除与更新可追踪 |

没有脱离场景的“最佳方案”。先写清数据规模、读写比例、故障模型和延迟目标，再做选择。

---

## 6. 放进真实系统，还要考虑什么

- 多路召回提高覆盖，也增加重复、延迟和融合调参成本。
- 重排模型应在真实领域数据上验证，通用排行榜效果不能直接代表线上。
- 多查询适合复杂问题，简单事实查询会徒增成本。

### 上线前检查

- 文档、chunk、embedding、索引和提示版本可追踪。
- 权限在检索阶段过滤，不能只靠生成阶段提醒模型保密。
- 分别评估召回、排序、事实性、引用精度和无答案拒答。
- 建立删除、更新、反馈和错误证据回流的数据闭环。

---

## 7. 常见误区与排查

### 容易踩的坑

1. 直接线性相加 BM25 与向量分数，量纲不同导致一路完全支配。
2. 重排只看片段文本，没有标题、时间和权限元数据。
3. 查询改写删除了否定词或关键约束，召回结果方向相反。

### 出现问题时，按这个顺序看

1. **还原现场**：确认受影响的请求、数据、时间窗，以及最近是否有发布或流量变化。
2. **沿链路定位**：从“理解查询意图”一路检查到“按证据覆盖选择上下文”，找出状态第一次偏离预期的位置。
3. **验证修复**：用最小复现、压力测试或故障注入证明问题消失，同时保留回滚方案。

---

## 8. 最后做一次复盘

> **一句话**：高质量检索通常把 BM25 等词法召回与向量召回融合，再用重排模型对较小候选集精排。
>
> **主链路**：理解查询意图 → 生成词法与语义检索请求 → BM25 和向量召回并集 → RRF / Cross-Encoder 重排 → 按证据覆盖选择上下文
>
> **关键状态**：BM25 和向量召回并集
>
> **最容易踩的坑**：直接线性相加 BM25 与向量分数，量纲不同导致一路完全支配。

---

## 9. 高频追问

### Q1. 请用一分钟说明混合检索、重排与查询改写的核心目标与工作机制。

高质量检索通常把 BM25 等词法召回与向量召回融合，再用重排模型对较小候选集精排。查询改写可补全缩写、拆分多跳问题或生成多个检索视角，但必须保留原意；完整链路应分别评估召回、重排和最终答案，不能只看一次相似度。

### Q2. 混合召回的核心机制是什么？

词法检索擅长专有名词、编号和精确短语，向量检索擅长语义改写。可用 Reciprocal Rank Fusion 等方法合并不同分数量纲的排名，并对字段和来源设权重。

### Q3. 重排为什么重要，实际如何落地？

Cross-encoder 或生成式重排器联合读取 query 与候选，相关性通常更高但成本更大。先宽召回几十到几百条，再重排到生成所需少量证据，并设置超时降级。

### Q4. 混合检索、重排与查询改写在工程落地时如何做取舍？

多路召回提高覆盖，也增加重复、延迟和融合调参成本；重排模型应在真实领域数据上验证，通用排行榜效果不能直接代表线上；多查询适合复杂问题，简单事实查询会徒增成本。

### Q5. 混合检索、重排与查询改写最常见的故障与排查重点是什么？

直接线性相加 BM25 与向量分数，量纲不同导致一路完全支配；重排只看片段文本，没有标题、时间和权限元数据；查询改写删除了否定词或关键约束，召回结果方向相反。排查时应关联运行状态、模型请求、工具调用、外部证据和最终结果，先判断是数据、模型、编排、权限还是依赖故障。
