---
slug: agent-deployment-release-rollback
title: 部署、灰度与版本回滚
---
# 部署、灰度与版本回滚

> **先记住**：Agent 发布物不仅是代码，还包括模型、系统提示、工作流、工具 schema、检索索引和安全策略。

---

## 1. 它在解决什么问题？

Agent 发布物不仅是代码，还包括模型、系统提示、工作流、工具 schema、检索索引和安全策略。部署必须为这些依赖建立组合版本，通过离线回归、影子流量、灰度和在线指标逐步放量；回滚要恢复兼容组合，并正确处理仍在运行的长任务。

读这一节时，先带着三个问题：

1. **核心对象是什么？** 哪些状态会在处理过程中发生变化？
2. **主链路怎么走？** 一次处理从哪里开始，到哪里才算结束？
3. **边界在哪里？** 数据量、并发或故障出现后，哪一环最先承压？

---

## 2. 先看全貌

![灰度发布通过流量路由控制版本](./assets/gateway_flow.svg)

<p class="diagram-caption">先建立整体位置感：版本化提示、模型、工具和工作流 → 离线评测通过门禁 → 小流量灰度并影子对比 → 异常自动停止并回滚整套配置</p>

### 主链路

```text
[版本化提示、模型、工具和工作流]
    │
    ▼
[离线评测通过门禁]
    │
    ▼
[小流量灰度并影子对比]
    │
    ▼
[监控质量、安全、成本与延迟]
    │
    ▼
[异常自动停止并回滚整套配置]
```

先把这条链路记住即可。接下来再逐层拆开每一步为什么这样设计。

---

## 3. 核心机制，逐层拆开

### 版本清单

每个 run 记录代码、提示、模型、路由、工具、策略、知识快照和评测版本。制品不可变，配置变更也走审查与发布流程，避免线上状态无法复现。

### 灰度验证

先离线评测和安全测试，再用影子流量比较但禁止真实副作用，随后按租户或比例灰度。守门指标包含质量、延迟、成本、拒绝率和工具错误，而非只看服务 5xx。

### 兼容与回滚

运行中任务通常固定原工作流与 schema 到结束；数据库和状态迁移采用向前/向后兼容。回滚同时处理提示、工具和索引版本，必要时暂停新任务并排空旧 worker。

---

## 4. 用一段实现把概念落地

下面只保留最关键的主干。阅读时，把代码与上一节的流程一一对应：

```yaml
release:
  bundle: {prompt: p17, workflow: w9, tools: t31, modelPolicy: m6}
  offlineGate: {successRate: 0.90, safetyRate: 1.0}
  rollout:
    - traffic: 1%
      hold: 30m
    - traffic: 10%
      hold: 2h
    - traffic: 50%
  rollbackOn:
    - success_rate_drop > 3%
    - p95_latency_increase > 20%
    - safety_incident_count > 0
```

### 对照着看

- **从哪里开始**：版本化提示、模型、工具和工作流
- **关键状态变化**：小流量灰度并影子对比
- **怎样才算完成**：异常自动停止并回滚整套配置

---

## 5. 怎么选才不容易错

| 关注点 | 怎么理解 | 怎么验证 |
|---|---|---|
| **状态持久化** | 会话展示状态与任务执行状态分离 | 断线、重启和 Worker 接管都不丢 run |
| **预算** | 成本、时间、步骤和并行度同时设上限 | 关注每个成功任务成本而非单次 token |
| **路由** | 模型、工具和工作流作为一个版本化 bundle | 降级必须保持安全与输出契约 |
| **观测** | run / step / span / 业务键可关联 | 质量、安全、成本和延迟一起告警 |

没有脱离场景的“最佳方案”。先写清数据规模、读写比例、故障模型和延迟目标，再做选择。

---

## 6. 放进真实系统，还要考虑什么

- 快速模型别名升级方便，却降低可复现性，高风险功能宜固定版本。
- 影子请求可验证模型输出，但对收费和数据共享仍要受配额与合规约束。
- 全量回滚简单，局部租户回滚更灵活但版本矩阵复杂。

### 上线前检查

- API 请求与长任务解耦，队列、租约、心跳和取消均持久化。
- run、step、模型、工具和业务结果使用同一 trace 关联。
- 路由和缓存以质量门槛为前提，不能用便宜结果静默降级。
- 灰度发布 bundle 化，异常能自动停止流量并整体回滚。

---

## 7. 常见误区与排查

### 容易踩的坑

1. 只回滚应用代码，工具 schema 和提示仍是新版本。
2. 新 worker 无法读取旧检查点，长任务全部失败。
3. 灰度指标只看基础设施健康，任务成功率已明显下降却继续放量。

### 出现问题时，按这个顺序看

1. **还原现场**：确认受影响的请求、数据、时间窗，以及最近是否有发布或流量变化。
2. **沿链路定位**：从“版本化提示、模型、工具和工作流”一路检查到“异常自动停止并回滚整套配置”，找出状态第一次偏离预期的位置。
3. **验证修复**：用最小复现、压力测试或故障注入证明问题消失，同时保留回滚方案。

---

## 8. 最后做一次复盘

> **一句话**：Agent 发布物不仅是代码，还包括模型、系统提示、工作流、工具 schema、检索索引和安全策略。
>
> **主链路**：版本化提示、模型、工具和工作流 → 离线评测通过门禁 → 小流量灰度并影子对比 → 监控质量、安全、成本与延迟 → 异常自动停止并回滚整套配置
>
> **关键状态**：小流量灰度并影子对比
>
> **最容易踩的坑**：只回滚应用代码，工具 schema 和提示仍是新版本。

---

## 9. 高频追问

### Q1. 请用一分钟说明部署、灰度与版本回滚的核心目标与工作机制。

Agent 发布物不仅是代码，还包括模型、系统提示、工作流、工具 schema、检索索引和安全策略。部署必须为这些依赖建立组合版本，通过离线回归、影子流量、灰度和在线指标逐步放量；回滚要恢复兼容组合，并正确处理仍在运行的长任务。

### Q2. 版本清单的核心机制是什么？

每个 run 记录代码、提示、模型、路由、工具、策略、知识快照和评测版本。制品不可变，配置变更也走审查与发布流程，避免线上状态无法复现。

### Q3. 灰度验证为什么重要，实际如何落地？

先离线评测和安全测试，再用影子流量比较但禁止真实副作用，随后按租户或比例灰度。守门指标包含质量、延迟、成本、拒绝率和工具错误，而非只看服务 5xx。

### Q4. 部署、灰度与版本回滚在工程落地时如何做取舍？

快速模型别名升级方便，却降低可复现性，高风险功能宜固定版本；影子请求可验证模型输出，但对收费和数据共享仍要受配额与合规约束；全量回滚简单，局部租户回滚更灵活但版本矩阵复杂。

### Q5. 部署、灰度与版本回滚最常见的故障与排查重点是什么？

只回滚应用代码，工具 schema 和提示仍是新版本；新 worker 无法读取旧检查点，长任务全部失败；灰度指标只看基础设施健康，任务成功率已明显下降却继续放量。排查时应关联运行状态、模型请求、工具调用、外部证据和最终结果，先判断是数据、模型、编排、权限还是依赖故障。
