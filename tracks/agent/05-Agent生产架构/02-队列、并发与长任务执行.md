---
slug: agent-queue-concurrency-long-running
title: 队列、并发与长任务执行
---
# 队列、并发与长任务执行

> **先记住**：长时间 Agent 任务应通过持久队列和 worker 执行，使用租约、心跳、幂等和检查点实现至少一次交付下的正确处理。

---

## 1. 它在解决什么问题？

长时间 Agent 任务应通过持久队列和 worker 执行，使用租约、心跳、幂等和检查点实现至少一次交付下的正确处理。并发控制不仅限制 worker 数，还要限制每租户、模型、工具和下游资源；任务取消、优先级、公平性和死信处理是生产必需能力。

读这一节时，先带着三个问题：

1. **核心对象是什么？** 哪些状态会在处理过程中发生变化？
2. **主链路怎么走？** 一次处理从哪里开始，到哪里才算结束？
3. **边界在哪里？** 数据量、并发或故障出现后，哪一环最先承压？

---

## 2. 先看全貌

![长任务需要跨组件追踪排队与执行](./assets/otel_architecture.png)

<p class="diagram-caption">先建立整体位置感：请求转换为持久任务 → 队列按租户和优先级调度 → Worker 领取带租约的任务 → 完成后原子写结果并确认消息</p>

### 主链路

```text
[请求转换为持久任务]
    │
    ▼
[队列按租户和优先级调度]
    │
    ▼
[Worker 领取带租约的任务]
    │
    ▼
[心跳、检查点与取消持续生效]
    │
    ▼
[完成后原子写结果并确认消息]
```

先把这条链路记住即可。接下来再逐层拆开每一步为什么这样设计。

---

## 3. 核心机制，逐层拆开

### 投递与租约

队列通常提供至少一次投递，worker 领取任务后持有可续租 lease 并定期心跳。租约到期允许其他 worker 接管，所以每个 step 必须幂等或能核对外部结果。

### 并发与背压

按租户配额、模型 RPM/TPM、工具容量和数据库连接设置分层 semaphore。队列深度和预计等待时间触发背压，入口可拒绝、降级或转异步通知。

### 长任务控制

任务支持暂停、取消、超时、优先级和检查点；取消信号传播到子任务与工具。失败超过阈值进入死信队列，保留状态、版本和人工重放入口。

---

## 4. 用一段实现把概念落地

下面只保留最关键的主干。阅读时，把代码与上一节的流程一一对应：

```python
task = queue.claim(worker_id, lease_seconds=30)
while not task.done:
    cancellation.raise_if_requested(task.run_id)
    result = execute_next_step(task)
    checkpoints.save(task.run_id, result, expected_version=task.version)
    queue.renew_lease(task.id, worker_id, lease_seconds=30)
queue.ack(task.id, worker_id)

# Worker 崩溃后租约到期，其他 Worker 从最后检查点接管。
```

### 对照着看

- **从哪里开始**：请求转换为持久任务
- **关键状态变化**：Worker 领取带租约的任务
- **怎样才算完成**：完成后原子写结果并确认消息

---

## 5. 怎么选才不容易错

| 关注点 | 怎么理解 | 怎么验证 |
|---|---|---|
| **状态持久化** | 会话展示状态与任务执行状态分离 | 断线、重启和 Worker 接管都不丢 run |
| **预算** | 成本、时间、步骤和并行度同时设上限 | 关注每个成功任务成本而非单次 token |
| **路由** | 模型、工具和工作流作为一个版本化 bundle | 降级必须保持安全与输出契约 |
| **观测** | run / step / span / 业务键可关联 | 质量、安全、成本和延迟一起告警 |

没有脱离场景的“最佳方案”。先写清数据规模、读写比例、故障模型和延迟目标，再做选择。

---

## 6. 放进真实系统，还要考虑什么

- 严格 FIFO 简单却可能让大任务阻塞小任务，可使用优先级和加权公平队列。
- 预取提高吞吐，但 worker 崩溃会延长任务不可见时间。
- 并行工具调用降低延迟，也可能击穿共享下游，需要总量预算。

### 上线前检查

- API 请求与长任务解耦，队列、租约、心跳和取消均持久化。
- run、step、模型、工具和业务结果使用同一 trace 关联。
- 路由和缓存以质量门槛为前提，不能用便宜结果静默降级。
- 灰度发布 bundle 化，异常能自动停止流量并整体回滚。

---

## 7. 常见误区与排查

### 容易踩的坑

1. worker 执行时间超过可见性超时，同一任务被重复消费。
2. 高消费租户占满队列，其他租户长期饥饿。
3. 取消只更新数据库，正在运行的浏览器和模型请求未停止。

### 出现问题时，按这个顺序看

1. **还原现场**：确认受影响的请求、数据、时间窗，以及最近是否有发布或流量变化。
2. **沿链路定位**：从“请求转换为持久任务”一路检查到“完成后原子写结果并确认消息”，找出状态第一次偏离预期的位置。
3. **验证修复**：用最小复现、压力测试或故障注入证明问题消失，同时保留回滚方案。

---

## 8. 最后做一次复盘

> **一句话**：长时间 Agent 任务应通过持久队列和 worker 执行，使用租约、心跳、幂等和检查点实现至少一次交付下的正确处理。
>
> **主链路**：请求转换为持久任务 → 队列按租户和优先级调度 → Worker 领取带租约的任务 → 心跳、检查点与取消持续生效 → 完成后原子写结果并确认消息
>
> **关键状态**：Worker 领取带租约的任务
>
> **最容易踩的坑**：worker 执行时间超过可见性超时，同一任务被重复消费。

---

## 9. 高频追问

### Q1. 请用一分钟说明队列、并发与长任务执行的核心目标与工作机制。

长时间 Agent 任务应通过持久队列和 worker 执行，使用租约、心跳、幂等和检查点实现至少一次交付下的正确处理。并发控制不仅限制 worker 数，还要限制每租户、模型、工具和下游资源；任务取消、优先级、公平性和死信处理是生产必需能力。

### Q2. 投递与租约的核心机制是什么？

队列通常提供至少一次投递，worker 领取任务后持有可续租 lease 并定期心跳。租约到期允许其他 worker 接管，所以每个 step 必须幂等或能核对外部结果。

### Q3. 并发与背压为什么重要，实际如何落地？

按租户配额、模型 RPM/TPM、工具容量和数据库连接设置分层 semaphore。队列深度和预计等待时间触发背压，入口可拒绝、降级或转异步通知。

### Q4. 队列、并发与长任务执行在工程落地时如何做取舍？

严格 FIFO 简单却可能让大任务阻塞小任务，可使用优先级和加权公平队列；预取提高吞吐，但 worker 崩溃会延长任务不可见时间；并行工具调用降低延迟，也可能击穿共享下游，需要总量预算。

### Q5. 队列、并发与长任务执行最常见的故障与排查重点是什么？

worker 执行时间超过可见性超时，同一任务被重复消费；高消费租户占满队列，其他租户长期饥饿；取消只更新数据库，正在运行的浏览器和模型请求未停止。排查时应关联运行状态、模型请求、工具调用、外部证据和最终结果，先判断是数据、模型、编排、权限还是依赖故障。
