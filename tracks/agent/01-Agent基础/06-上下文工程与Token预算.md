---
slug: agent-context-engineering
title: 上下文工程与 Token 预算
---
# 上下文工程与 Token 预算

> **先记住**：上下文工程是在有限窗口内选择、组织和标注最有用的信息，包括系统规则、任务状态、工具定义、检索证据、记忆和历史。

---

## 1. 它在解决什么问题？

上下文工程是在有限窗口内选择、组织和标注最有用的信息，包括系统规则、任务状态、工具定义、检索证据、记忆和历史。目标不是塞满窗口，而是提高信噪比、保持指令边界，并在质量、延迟和成本之间进行可测量的预算分配。

读这一节时，先带着三个问题：

1. **核心对象是什么？** 哪些状态会在处理过程中发生变化？
2. **主链路怎么走？** 一次处理从哪里开始，到哪里才算结束？
3. **边界在哪里？** 数据量、并发或故障出现后，哪一环最先承压？

---

## 2. 先看全貌

![检索证据只是上下文的一部分](./assets/rag_diagram.png)

<p class="diagram-caption">先建立整体位置感：收集系统规则、任务与证据 → 按优先级和预算选择上下文 → 压缩历史但保留约束 → 记录 token、命中率与遗漏错误</p>

### 主链路

```text
[收集系统规则、任务与证据]
    │
    ▼
[按优先级和预算选择上下文]
    │
    ▼
[压缩历史但保留约束]
    │
    ▼
[模型执行当前步骤]
    │
    ▼
[记录 token、命中率与遗漏错误]
```

先把这条链路记住即可。接下来再逐层拆开每一步为什么这样设计。

---

## 3. 核心机制，逐层拆开

### 上下文分层

稳定规则、用户目标、运行状态、外部证据和工具结果应使用清晰边界与结构化标签。低信任内容只能作为数据，不应与系统指令混合，来源和时间要随证据一起传递。

### 压缩与选择

优先删除重复和无关内容，再做分层摘要；当前决策依赖的原始证据保留引用。可按最近性、相关性、权威性和任务阶段动态选择，而非固定截取最后 N 条消息。

### 预算与缓存

为系统提示、历史、检索、工具输出和生成分别设 token 上限。稳定前缀可利用提示缓存，长工具结果先结构化提取，预算耗尽时应降级或请求用户缩小范围。

---

## 4. 用一段实现把概念落地

下面只保留最关键的主干。阅读时，把代码与上一节的流程一一对应：

```python
def build_context(system, task, evidence, history, budget):
    fixed = tokenize(system) + tokenize(task)
    remaining = budget - len(fixed) - RESERVED_OUTPUT_TOKENS
    ranked = rerank(evidence, query=task, authority_first=True)
    selected = pack_without_splitting_citations(ranked, remaining * 0.65)
    summary = summarize_history(history, remaining * 0.35, keep_constraints=True)
    return fixed + tokenize(selected) + tokenize(summary)
```

### 对照着看

- **从哪里开始**：收集系统规则、任务与证据
- **关键状态变化**：压缩历史但保留约束
- **怎样才算完成**：记录 token、命中率与遗漏错误

---

## 5. 怎么选才不容易错

| 关注点 | 怎么理解 | 怎么验证 |
|---|---|---|
| **自主性** | 只在路径无法枚举时让模型动态决策 | 稳定路径优先 workflow，开放局部才用 loop |
| **状态** | 显式保存目标、步骤、观察、预算与终止条件 | 状态可序列化、可恢复、可审计 |
| **上下文** | 区分规则、任务、证据、记忆和工具结果 | 按优先级装配并测量遗漏与污染 |
| **终止** | 成功、失败、取消、超时和预算耗尽都有明确出口 | 检测重复动作与无进展循环 |

没有脱离场景的“最佳方案”。先写清数据规模、读写比例、故障模型和延迟目标，再做选择。

---

## 6. 放进真实系统，还要考虑什么

- 更长上下文不必然更好，中间信息遗失和噪声会降低决策质量。
- 摘要模型节约主模型成本，但引入额外延迟和摘要错误。
- 工具 schema 太多会占窗口并降低选择准确率，应按状态动态暴露。

### 上线前检查

- 显式设置步骤、token、费用、时间与工具调用上限。
- 模型不直接拥有权限；所有工具调用经过 Schema 与策略校验。
- 状态、动作、观察和终止原因可持久化与重放。
- 检测重复动作、无进展与上下文污染，必要时请求人工接管。

---

## 7. 常见误区与排查

### 容易踩的坑

1. 截断时删除了系统约束或用户最新修改。
2. 把网页中的提示注入文本放入高优先级指令区。
3. 工具返回整份日志或数据库结果，挤掉真正的任务证据。

### 出现问题时，按这个顺序看

1. **还原现场**：确认受影响的请求、数据、时间窗，以及最近是否有发布或流量变化。
2. **沿链路定位**：从“收集系统规则、任务与证据”一路检查到“记录 token、命中率与遗漏错误”，找出状态第一次偏离预期的位置。
3. **验证修复**：用最小复现、压力测试或故障注入证明问题消失，同时保留回滚方案。

---

## 8. 最后做一次复盘

> **一句话**：上下文工程是在有限窗口内选择、组织和标注最有用的信息，包括系统规则、任务状态、工具定义、检索证据、记忆和历史。
>
> **主链路**：收集系统规则、任务与证据 → 按优先级和预算选择上下文 → 压缩历史但保留约束 → 模型执行当前步骤 → 记录 token、命中率与遗漏错误
>
> **关键状态**：压缩历史但保留约束
>
> **最容易踩的坑**：截断时删除了系统约束或用户最新修改。

---

## 9. 高频追问

### Q1. 请用一分钟说明上下文工程与 Token 预算的核心目标与工作机制。

上下文工程是在有限窗口内选择、组织和标注最有用的信息，包括系统规则、任务状态、工具定义、检索证据、记忆和历史。目标不是塞满窗口，而是提高信噪比、保持指令边界，并在质量、延迟和成本之间进行可测量的预算分配。

### Q2. 上下文分层的核心机制是什么？

稳定规则、用户目标、运行状态、外部证据和工具结果应使用清晰边界与结构化标签。低信任内容只能作为数据，不应与系统指令混合，来源和时间要随证据一起传递。

### Q3. 压缩与选择为什么重要，实际如何落地？

优先删除重复和无关内容，再做分层摘要；当前决策依赖的原始证据保留引用。可按最近性、相关性、权威性和任务阶段动态选择，而非固定截取最后 N 条消息。

### Q4. 上下文工程与 Token 预算在工程落地时如何做取舍？

更长上下文不必然更好，中间信息遗失和噪声会降低决策质量；摘要模型节约主模型成本，但引入额外延迟和摘要错误；工具 schema 太多会占窗口并降低选择准确率，应按状态动态暴露。

### Q5. 上下文工程与 Token 预算最常见的故障与排查重点是什么？

截断时删除了系统约束或用户最新修改；把网页中的提示注入文本放入高优先级指令区；工具返回整份日志或数据库结果，挤掉真正的任务证据。排查时应关联运行状态、模型请求、工具调用、外部证据和最终结果，先判断是数据、模型、编排、权限还是依赖故障。
