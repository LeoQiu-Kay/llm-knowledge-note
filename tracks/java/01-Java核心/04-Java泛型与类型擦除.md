---
slug: java-generics-erasure
title: Java 泛型与类型擦除
---
# Java 泛型与类型擦除

> **先记住**：Java 泛型在编译期提供类型检查和复用，运行期大部分类型参数会被擦除为上界，并通过强制转换和桥接方法保持多态。

---

## 1. 它在解决什么问题？

Java 泛型在编译期提供类型检查和复用，运行期大部分类型参数会被擦除为上界，并通过强制转换和桥接方法保持多态。理解 PECS、通配符捕获、泛型不变性和堆污染，才能正确设计集合 API、反射代码和序列化边界。

读这一节时，先带着三个问题：

1. **核心对象是什么？** 哪些状态会在处理过程中发生变化？
2. **主链路怎么走？** 一次处理从哪里开始，到哪里才算结束？
3. **边界在哪里？** 数据量、并发或故障出现后，哪一环最先承压？

---

## 2. 先看全貌

![集合 API 中的泛型接口层次](./assets/java_collections.png)

<p class="diagram-caption">先建立整体位置感：源码声明类型参数 → 编译器执行类型检查与推断 → 字节码擦除为上界并插入强转 → 运行期通过反射保留有限签名信息</p>

### 主链路

```text
[源码声明类型参数]
    │
    ▼
[编译器执行类型检查与推断]
    │
    ▼
[字节码擦除为上界并插入强转]
    │
    ▼
[必要时生成桥接方法]
    │
    ▼
[运行期通过反射保留有限签名信息]
```

先把这条链路记住即可。接下来再逐层拆开每一步为什么这样设计。

---

## 3. 核心机制，逐层拆开

### 类型擦除

编译器把类型参数擦除为 Object 或声明的上界，并在读取位置插入转换。不同参数化类型通常共享同一个运行时 Class，因此不能直接 new T、创建 T[] 或用 instanceof 判断具体类型参数。

### 泛型不变性与 PECS

List<Integer> 不是 List<Number> 的子类型。只生产 T 的参数用 ? extends T，只消费 T 的参数用 ? super T；读写兼有时通常直接使用明确的类型参数。

### 桥接方法与堆污染

编译器可能生成 bridge method 保持擦除后的重写关系。原生类型、错误的可变参数和不安全强转会让非目标类型进入泛型容器，最终在远离根因的位置触发 ClassCastException。

---

## 4. 用一段实现把概念落地

下面只保留最关键的主干。阅读时，把代码与上一节的流程一一对应：

```java
// PECS：生产者 extends，消费者 super。
static <T> void copy(
        List<? extends T> source,
        List<? super T> target) {
    for (T value : source) {
        target.add(value);
    }
}

List<Integer> source = List.of(1, 2, 3);
List<Number> target = new ArrayList<>();
copy(source, target);
```

### 对照着看

- **从哪里开始**：源码声明类型参数
- **关键状态变化**：字节码擦除为上界并插入强转
- **怎样才算完成**：运行期通过反射保留有限签名信息

---

## 5. 怎么选才不容易错

| 关注点 | 怎么理解 | 怎么验证 |
|---|---|---|
| **语义** | 先确定顺序、唯一性、键值、对象身份或异常边界 | 用接口表达约束，不让调用方依赖偶然实现 |
| **复杂度** | 同时说平均、最坏和触发退化的条件 | 用真实数据规模和访问分布压测 |
| **内存** | 关注对象头、引用、扩容、装箱和临时对象 | 观察分配率、存活集与 GC 压力 |
| **并发** | 区分可见性、原子性、不可变与容器线程安全 | 优先缩小共享状态，再选同步原语 |

没有脱离场景的“最佳方案”。先写清数据规模、读写比例、故障模型和延迟目标，再做选择。

---

## 6. 放进真实系统，还要考虑什么

- 公共 API 优先表达最小必要能力，避免通配符层级过深导致调用方难以推断类型。
- 需要保留运行期泛型信息时使用显式 Class、TypeToken 或框架提供的类型引用，而不是假设反射能恢复全部信息。
- 对 @SafeVarargs 的使用必须能证明方法不会写入或泄漏不安全的泛型数组。

### 上线前检查

- 写清 API 的空值、异常、顺序与线程安全契约。
- 给复杂度时补充数据规模、最坏路径和内存代价。
- 用单元测试覆盖 equals/hashCode、资源关闭和边界输入。
- 热点代码再用 JFR / profiler 验证，不凭直觉微优化。

---

## 7. 常见误区与排查

### 容易踩的坑

1. 混用原生类型绕过编译检查，线上出现延迟 ClassCastException。
2. 错误使用 ? extends 后尝试写入，或错误使用 ? super 后假设能读取具体子类型。
3. JSON 反序列化只传 List.class，元素退化为 Map 而不是目标类型。

### 出现问题时，按这个顺序看

1. **还原现场**：确认受影响的请求、数据、时间窗，以及最近是否有发布或流量变化。
2. **沿链路定位**：从“源码声明类型参数”一路检查到“运行期通过反射保留有限签名信息”，找出状态第一次偏离预期的位置。
3. **验证修复**：用最小复现、压力测试或故障注入证明问题消失，同时保留回滚方案。

---

## 8. 最后做一次复盘

> **一句话**：Java 泛型在编译期提供类型检查和复用，运行期大部分类型参数会被擦除为上界，并通过强制转换和桥接方法保持多态。
>
> **主链路**：源码声明类型参数 → 编译器执行类型检查与推断 → 字节码擦除为上界并插入强转 → 必要时生成桥接方法 → 运行期通过反射保留有限签名信息
>
> **关键状态**：字节码擦除为上界并插入强转
>
> **最容易踩的坑**：混用原生类型绕过编译检查，线上出现延迟 ClassCastException。

---

## 9. 高频追问

### Q1. 请用一分钟说明Java 泛型与类型擦除的核心目标与工作机制。

Java 泛型在编译期提供类型检查和复用，运行期大部分类型参数会被擦除为上界，并通过强制转换和桥接方法保持多态。理解 PECS、通配符捕获、泛型不变性和堆污染，才能正确设计集合 API、反射代码和序列化边界。

### Q2. 类型擦除的核心机制是什么？

编译器把类型参数擦除为 Object 或声明的上界，并在读取位置插入转换。不同参数化类型通常共享同一个运行时 Class，因此不能直接 new T、创建 T[] 或用 instanceof 判断具体类型参数。

### Q3. 泛型不变性与 PECS为什么重要，实际如何落地？

List<Integer> 不是 List<Number> 的子类型。只生产 T 的参数用 ? extends T，只消费 T 的参数用 ? super T；读写兼有时通常直接使用明确的类型参数。

### Q4. Java 泛型与类型擦除在工程落地时如何做取舍？

公共 API 优先表达最小必要能力，避免通配符层级过深导致调用方难以推断类型；需要保留运行期泛型信息时使用显式 Class、TypeToken 或框架提供的类型引用，而不是假设反射能恢复全部信息；对 @SafeVarargs 的使用必须能证明方法不会写入或泄漏不安全的泛型数组。

### Q5. Java 泛型与类型擦除最常见的故障与排查重点是什么？

混用原生类型绕过编译检查，线上出现延迟 ClassCastException；错误使用 ? extends 后尝试写入，或错误使用 ? super 后假设能读取具体子类型；JSON 反序列化只传 List.class，元素退化为 Map 而不是目标类型。排查时应先用指标和日志确认现象，再缩小到线程、资源、依赖或数据边界。
