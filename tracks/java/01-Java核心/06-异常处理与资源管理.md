---
slug: java-exception-resource
title: 异常处理与资源管理
---
# 异常处理与资源管理

> **先记住**：异常用于表达无法在当前层正常完成的控制流。

---

## 1. 它在解决什么问题？

异常用于表达无法在当前层正常完成的控制流。工程上要区分可恢复业务错误、参数错误和系统故障，保持异常因果链，在正确边界转换协议错误；文件、连接和锁等资源必须用 try-with-resources 或 finally 确保释放。

读这一节时，先带着三个问题：

1. **核心对象是什么？** 哪些状态会在处理过程中发生变化？
2. **主链路怎么走？** 一次处理从哪里开始，到哪里才算结束？
3. **边界在哪里？** 数据量、并发或故障出现后，哪一环最先承压？

---

## 2. 先看全貌

![异常沿线程调用栈传播的运行时背景](./assets/jvm_architecture.png)

<p class="diagram-caption">先建立整体位置感：业务代码抛出异常 → 调用栈逐层寻找处理器 → 捕获后分类、转换或恢复 → 日志保留根因与业务上下文</p>

### 主链路

```text
[业务代码抛出异常]
    │
    ▼
[调用栈逐层寻找处理器]
    │
    ▼
[捕获后分类、转换或恢复]
    │
    ▼
[finally / close 释放资源]
    │
    ▼
[日志保留根因与业务上下文]
```

先把这条链路记住即可。接下来再逐层拆开每一步为什么这样设计。

---

## 3. 核心机制，逐层拆开

### 受检与非受检异常

受检异常强制调用方处理，适合确实可恢复且需要显式决策的场景；非受检异常适合编程错误和无法局部恢复的失败。关键不是类别本身，而是调用方是否有合理恢复动作。

### 异常链与边界转换

捕获异常时应保留 cause，并在领域、应用、接口边界转换为稳定错误码。日志只在能够增加上下文或最终处理的层记录，避免同一异常被重复打印。

### try-with-resources

实现 AutoCloseable 的资源可自动按逆序关闭；主异常与 close 异常通过 suppressed exceptions 同时保留。数据库连接、流、锁封装和临时文件都应有明确生命周期。

---

## 4. 用一段实现把概念落地

下面只保留最关键的主干。阅读时，把代码与上一节的流程一一对应：

```java
try (Connection connection = dataSource.getConnection();
     PreparedStatement statement = connection.prepareStatement(SQL)) {
    statement.setString(1, userId);
    try (ResultSet rs = statement.executeQuery()) {
        return rs.next() ? map(rs) : Optional.empty();
    }
} catch (SQLException e) {
    // 保留 cause，并转换为调用方能理解的领域异常。
    throw new RepositoryException("load user failed: " + userId, e);
}
```

### 对照着看

- **从哪里开始**：业务代码抛出异常
- **关键状态变化**：捕获后分类、转换或恢复
- **怎样才算完成**：日志保留根因与业务上下文

---

## 5. 怎么选才不容易错

| 关注点 | 怎么理解 | 怎么验证 |
|---|---|---|
| **语义** | 先确定顺序、唯一性、键值、对象身份或异常边界 | 用接口表达约束，不让调用方依赖偶然实现 |
| **复杂度** | 同时说平均、最坏和触发退化的条件 | 用真实数据规模和访问分布压测 |
| **内存** | 关注对象头、引用、扩容、装箱和临时对象 | 观察分配率、存活集与 GC 压力 |
| **并发** | 区分可见性、原子性、不可变与容器线程安全 | 优先缩小共享状态，再选同步原语 |

没有脱离场景的“最佳方案”。先写清数据规模、读写比例、故障模型和延迟目标，再做选择。

---

## 6. 放进真实系统，还要考虑什么

- 批处理可选择逐项记录失败继续执行，交易链路则通常应快速失败并回滚。
- 错误响应要对用户稳定、对日志可诊断，同时避免泄露堆栈和内部路径。
- 重试只针对可判定的瞬时故障，并与超时、退避和幂等一起设计。

### 上线前检查

- 写清 API 的空值、异常、顺序与线程安全契约。
- 给复杂度时补充数据规模、最坏路径和内存代价。
- 用单元测试覆盖 equals/hashCode、资源关闭和边界输入。
- 热点代码再用 JFR / profiler 验证，不凭直觉微优化。

---

## 7. 常见误区与排查

### 容易踩的坑

1. catch Exception 后吞掉异常，调用方误以为成功。
2. 只记录 message 不保留 cause，丢失真正堆栈。
3. 手工 close 在中途异常时未执行，连接池逐渐耗尽。

### 出现问题时，按这个顺序看

1. **还原现场**：确认受影响的请求、数据、时间窗，以及最近是否有发布或流量变化。
2. **沿链路定位**：从“业务代码抛出异常”一路检查到“日志保留根因与业务上下文”，找出状态第一次偏离预期的位置。
3. **验证修复**：用最小复现、压力测试或故障注入证明问题消失，同时保留回滚方案。

---

## 8. 最后做一次复盘

> **一句话**：异常用于表达无法在当前层正常完成的控制流。
>
> **主链路**：业务代码抛出异常 → 调用栈逐层寻找处理器 → 捕获后分类、转换或恢复 → finally / close 释放资源 → 日志保留根因与业务上下文
>
> **关键状态**：捕获后分类、转换或恢复
>
> **最容易踩的坑**：catch Exception 后吞掉异常，调用方误以为成功。

---

## 9. 高频追问

### Q1. 请用一分钟说明异常处理与资源管理的核心目标与工作机制。

异常用于表达无法在当前层正常完成的控制流。工程上要区分可恢复业务错误、参数错误和系统故障，保持异常因果链，在正确边界转换协议错误；文件、连接和锁等资源必须用 try-with-resources 或 finally 确保释放。

### Q2. 受检与非受检异常的核心机制是什么？

受检异常强制调用方处理，适合确实可恢复且需要显式决策的场景；非受检异常适合编程错误和无法局部恢复的失败。关键不是类别本身，而是调用方是否有合理恢复动作。

### Q3. 异常链与边界转换为什么重要，实际如何落地？

捕获异常时应保留 cause，并在领域、应用、接口边界转换为稳定错误码。日志只在能够增加上下文或最终处理的层记录，避免同一异常被重复打印。

### Q4. 异常处理与资源管理在工程落地时如何做取舍？

批处理可选择逐项记录失败继续执行，交易链路则通常应快速失败并回滚；错误响应要对用户稳定、对日志可诊断，同时避免泄露堆栈和内部路径；重试只针对可判定的瞬时故障，并与超时、退避和幂等一起设计。

### Q5. 异常处理与资源管理最常见的故障与排查重点是什么？

catch Exception 后吞掉异常，调用方误以为成功；只记录 message 不保留 cause，丢失真正堆栈；手工 close 在中途异常时未执行，连接池逐渐耗尽。排查时应先用指标和日志确认现象，再缩小到线程、资源、依赖或数据边界。
