---
slug: java-object-model
title: Java 对象模型与不可变性
---
# Java 对象模型与不可变性

> **先记住**：Java 对象以类定义行为和状态，引用变量保存的是对象引用而非对象本身。

---

## 1. 它在解决什么问题？

Java 对象以类定义行为和状态，引用变量保存的是对象引用而非对象本身。正确设计需要区分身份与值语义，遵守 equals/hashCode 契约，并通过 final 字段、防御性拷贝和受控构造建立不可变对象；不可变性能够显著降低并发共享和缓存键设计的复杂度。

读这一节时，先带着三个问题：

1. **核心对象是什么？** 哪些状态会在处理过程中发生变化？
2. **主链路怎么走？** 一次处理从哪里开始，到哪里才算结束？
3. **边界在哪里？** 数据量、并发或故障出现后，哪一环最先承压？

---

## 2. 先看全貌

![对象、线程栈与堆的运行时位置](./assets/jvm_architecture.png)

<p class="diagram-caption">先建立整体位置感：类定义字段和行为 → 构造器建立对象不变量 → 引用在方法间传递 → 不可变对象安全共享</p>

### 主链路

```text
[类定义字段和行为]
    │
    ▼
[构造器建立对象不变量]
    │
    ▼
[引用在方法间传递]
    │
    ▼
[equals / hashCode 定义值语义]
    │
    ▼
[不可变对象安全共享]
```

先把这条链路记住即可。接下来再逐层拆开每一步为什么这样设计。

---

## 3. 核心机制，逐层拆开

### 身份与值语义

默认 Object.equals 比较身份；值对象应按业务字段比较。重写 equals 时要满足自反、对称、传递、一致和非空性，并同步重写 hashCode，避免对象在 HashMap、HashSet 中出现查找和去重错误。

### 不可变对象

不可变对象在构造完成后状态不再变化，通常使用 final 类或限制继承、private final 字段、不暴露可变内部对象，并在构造和 getter 边界做防御性拷贝。

### 复制与对象图

浅拷贝只复制第一层字段引用，深拷贝需要复制完整可变对象图。业务中更推荐显式 copy factory、record 或映射层，而不是依赖脆弱的 Cloneable 和序列化拷贝。

---

## 4. 用一段实现把概念落地

下面只保留最关键的主干。阅读时，把代码与上一节的流程一一对应：

```java
public record Money(long cents, Currency currency) {
    public Money {
        Objects.requireNonNull(currency);
        if (cents < 0) throw new IllegalArgumentException("negative");
    }

    public Money add(Money other) {
        if (currency != other.currency) throw new IllegalArgumentException("currency");
        return new Money(Math.addExact(cents, other.cents), currency);
    }
}
```

### 对照着看

- **从哪里开始**：类定义字段和行为
- **关键状态变化**：引用在方法间传递
- **怎样才算完成**：不可变对象安全共享

---

## 5. 怎么选才不容易错

| 关注点 | 怎么理解 | 怎么验证 |
|---|---|---|
| **语义** | 先确定顺序、唯一性、键值、对象身份或异常边界 | 用接口表达约束，不让调用方依赖偶然实现 |
| **复杂度** | 同时说平均、最坏和触发退化的条件 | 用真实数据规模和访问分布压测 |
| **内存** | 关注对象头、引用、扩容、装箱和临时对象 | 观察分配率、存活集与 GC 压力 |
| **并发** | 区分可见性、原子性、不可变与容器线程安全 | 优先缩小共享状态，再选同步原语 |

没有脱离场景的“最佳方案”。先写清数据规模、读写比例、故障模型和延迟目标，再做选择。

---

## 6. 放进真实系统，还要考虑什么

- 值对象适合不可变设计，实体对象则通常需要稳定身份和受控状态迁移，不能把两者的 equals 语义混用。
- 防御性拷贝提高安全性但会增加分配，应优先缩小可变状态边界，而不是到处深拷贝。
- record 适合数据载体，但字段引用指向的集合仍可能可变，不能把语法简洁误认为深度不可变。

### 上线前检查

- 写清 API 的空值、异常、顺序与线程安全契约。
- 给复杂度时补充数据规模、最坏路径和内存代价。
- 用单元测试覆盖 equals/hashCode、资源关闭和边界输入。
- 热点代码再用 JFR / profiler 验证，不凭直觉微优化。

---

## 7. 常见误区与排查

### 容易踩的坑

1. 把可变字段用于 HashMap key，字段变化后再也无法按新旧值正确定位。
2. equals 使用可变集合或关联对象，导致递归、懒加载和性能问题。
3. getter 直接返回内部 List，调用方绕过校验修改对象状态。

### 出现问题时，按这个顺序看

1. **还原现场**：确认受影响的请求、数据、时间窗，以及最近是否有发布或流量变化。
2. **沿链路定位**：从“类定义字段和行为”一路检查到“不可变对象安全共享”，找出状态第一次偏离预期的位置。
3. **验证修复**：用最小复现、压力测试或故障注入证明问题消失，同时保留回滚方案。

---

## 8. 最后做一次复盘

> **一句话**：Java 对象以类定义行为和状态，引用变量保存的是对象引用而非对象本身。
>
> **主链路**：类定义字段和行为 → 构造器建立对象不变量 → 引用在方法间传递 → equals / hashCode 定义值语义 → 不可变对象安全共享
>
> **关键状态**：引用在方法间传递
>
> **最容易踩的坑**：把可变字段用于 HashMap key，字段变化后再也无法按新旧值正确定位。

---

## 9. 高频追问

### Q1. 请用一分钟说明Java 对象模型与不可变性的核心目标与工作机制。

Java 对象以类定义行为和状态，引用变量保存的是对象引用而非对象本身。正确设计需要区分身份与值语义，遵守 equals/hashCode 契约，并通过 final 字段、防御性拷贝和受控构造建立不可变对象；不可变性能够显著降低并发共享和缓存键设计的复杂度。

### Q2. 身份与值语义的核心机制是什么？

默认 Object.equals 比较身份；值对象应按业务字段比较。重写 equals 时要满足自反、对称、传递、一致和非空性，并同步重写 hashCode，避免对象在 HashMap、HashSet 中出现查找和去重错误。

### Q3. 不可变对象为什么重要，实际如何落地？

不可变对象在构造完成后状态不再变化，通常使用 final 类或限制继承、private final 字段、不暴露可变内部对象，并在构造和 getter 边界做防御性拷贝。

### Q4. Java 对象模型与不可变性在工程落地时如何做取舍？

值对象适合不可变设计，实体对象则通常需要稳定身份和受控状态迁移，不能把两者的 equals 语义混用；防御性拷贝提高安全性但会增加分配，应优先缩小可变状态边界，而不是到处深拷贝；record 适合数据载体，但字段引用指向的集合仍可能可变，不能把语法简洁误认为深度不可变。

### Q5. Java 对象模型与不可变性最常见的故障与排查重点是什么？

把可变字段用于 HashMap key，字段变化后再也无法按新旧值正确定位；equals 使用可变集合或关联对象，导致递归、懒加载和性能问题；getter 直接返回内部 List，调用方绕过校验修改对象状态。排查时应先用指标和日志确认现象，再缩小到线程、资源、依赖或数据边界。
