---
slug: java-concurrent-collections
title: 并发容器与原子类
---
# 并发容器与原子类

> **一句话定位**：并发容器通过分段、CAS、写时复制或无锁队列等机制提供明确的线程安全语义；原子类适合单变量或可表达为 CAS 循环的状态更新。

---

## 1. 先给结论

并发容器通过分段、CAS、写时复制或无锁队列等机制提供明确的线程安全语义；原子类适合单变量或可表达为 CAS 循环的状态更新。容器方法安全不代表多个方法组合后的业务操作原子。

### 面试答题路线

回答这类题不要从名词堆砌开始，按下面四步展开最稳：

1. **先定边界**：它解决什么问题，不解决什么问题。
2. **再讲机制**：把核心数据结构、状态机或调用链讲清楚。
3. **补充取舍**：说明复杂度、正确性、资源和可维护性的代价。
4. **落到生产**：给出超时、幂等、观测、压测与回滚方案。

---

## 2. 核心原理

### ConcurrentHashMap

读取通常不加全局锁，更新通过 CAS 与桶级同步完成；compute、merge 等方法可把单 key 的复合逻辑放进原子操作。

### CopyOnWrite 与队列

CopyOnWriteArrayList 用写时复制换取无锁快照遍历，适合读多写极少；BlockingQueue 同时承担缓冲和生产消费协调。

### CAS 与 ABA

CAS 比较旧值并条件更新，竞争失败重试；版本戳或不可复用节点可处理 ABA。LongAdder 通过分散热点提高高竞争计数吞吐，但读取不是瞬时原子快照。

### 2.1 一张图建立整体心智模型

![无锁结构中必须防住的竞态](./assets/lock_free_list.png)

> **图：无锁结构中必须防住的竞态**
> （图源与许可见 [assets/CREDITS.md](./assets/CREDITS.md)）
>
> 对照本文阅读时，把图中的输入、状态、执行器和输出依次映射为：
> **读取当前桶或槽位 → CAS 尝试无锁更新 → 冲突时锁定局部结构 → 原子复合 API 返回一致结果**。
> 图负责建立全局位置，具体正确性仍要回到本文的数据流、状态边界和失败路径。

---

## 3. 工作流程与关键路径

```text
[读取当前桶或槽位]
    │
    ▼
[CAS 尝试无锁更新]
    │
    ▼
[冲突时锁定局部结构]
    │
    ▼
[必要时协助扩容或重试]
    │
    ▼
[原子复合 API 返回一致结果]
```

### 3.1 每一步分别承担什么

- **入口与约束**：`读取当前桶或槽位` 决定后续处理的语义、权限和容量边界。
- **核心决策**：`CAS 尝试无锁更新` 与 `冲突时锁定局部结构` 是最容易答成“只会背名词”的地方，要讲清状态如何变化。
- **执行与副作用**：中间步骤必须说明失败是否可重试、是否会重复，以及资源何时释放。
- **完成条件**：`原子复合 API 返回一致结果` 不能只看“没有抛异常”，还要有业务结果、指标或持久化证据。

### 3.2 复杂度不只是一行大 O

面试里给出时间复杂度后，还要补三件事：**数据规模是否会放大常数项、最坏路径何时触发、资源上限在哪里**。生产环境更常被队列等待、锁竞争、网络、序列化、缓存失效或外部限流拖慢；因此复杂度分析必须和实际访问分布、尾延迟及容量模型一起讲。

---

## 4. 关键实现

下面的片段不是完整框架，而是把最关键的边界写出来：

```java
ConcurrentHashMap<String, LongAdder> counts = new ConcurrentHashMap<>();

void record(String key) {
    counts.computeIfAbsent(key, ignored -> new LongAdder()).increment();
}

long snapshot(String key) {
    LongAdder value = counts.get(key);
    return value == null ? 0 : value.sum();
}
```

### 4.1 实现时盯住的状态

| 状态 | 需要回答的问题 |
|---|---|
| 输入 | `读取当前桶或槽位` 是否已经校验语义、权限、大小与版本？ |
| 中间状态 | `冲突时锁定局部结构` 能否持久化、观测或在并发下保持不变量？ |
| 副作用 | 失败重试会不会重复执行？是否有幂等键、事务或补偿？ |
| 完成 | `原子复合 API 返回一致结果` 由什么证据确认？调用方能否区分成功、部分成功与失败？ |

---

## 5. 对比与选型

| 决策维度 | 面试中要说清 | 生产验证 |
|---|---|---|
| **正确性** | 先定义共享状态、不变量和 happens-before | 用压力测试、竞态测试和线程转储验证 |
| **吞吐** | 线程数不能突破 CPU、连接池和下游容量 | 观察排队时间而非只看完成量 |
| **延迟** | 区分锁等待、队列等待、I/O 等待和调度 | 记录 p95/p99 与超时、取消传播 |
| **故障隔离** | 任务必须有界、可取消、可拒绝 | 按依赖拆池，禁止无限队列与无限重试 |

真正的选型结论应带条件：**在什么规模、读写比例、故障模型、SLO 和团队维护能力下选择它**。离开约束谈“谁更快”“谁更高级”没有工程意义。

---

## 6. 工程实践

- 按访问模式选择容器，不能只看“线程安全”标签。
- compute 回调应短小且无外部阻塞，避免放大桶竞争。
- 跨多个 key 或多个容器的一致性仍需锁、事务或状态机。

### 6.1 落地检查表

- 队列、等待、重试和并发度全部有界。
- 中断、取消、超时沿调用链传播，finally 释放锁与资源。
- 按依赖隔离执行器，避免一个慢下游占满全部线程。
- 压测同时观察吞吐、排队、上下文切换、锁竞争和尾延迟。

---

## 7. 常见故障

1. 在 ConcurrentHashMap 上先 get 再 put，初始化被并发执行多次。
2. 写多场景误用 CopyOnWriteArrayList，分配和复制激增。
3. 把 LongAdder.sum 当作严格事务计数。

### 7.1 推荐排查顺序

1. **先确认现象**：影响哪些请求、数据、租户与时间窗，是否与发布或流量变化相关。
2. **再沿关键路径定位**：从 `读取当前桶或槽位` 逐步检查到 `原子复合 API 返回一致结果`，不要跳过排队、缓存、代理或外部依赖。
3. **区分原因和结果**：高 CPU、线程多、token 多、连接满可能只是症状；用日志、指标、Trace 和状态快照互相印证。
4. **最后验证修复**：用最小复现、压力或故障注入证明问题消失，同时保留一键回滚。

最常见的错误是：**在 ConcurrentHashMap 上先 get 再 put，初始化被并发执行多次。**


---

## 8. 最简记忆

```text
一句话：并发容器通过分段、CAS、写时复制或无锁队列等机制提供明确的线程安全语义；原子类适合单变量或可表达为 CAS 循环的状态更新。

主链路：读取当前桶或槽位 → CAS 尝试无锁更新 → 冲突时锁定局部结构 → 必要时协助扩容或重试 → 原子复合 API 返回一致结果
核心状态：冲突时锁定局部结构
完成证据：原子复合 API 返回一致结果
高频坑：在 ConcurrentHashMap 上先 get 再 put，初始化被并发执行多次。
```

---

## 🎯 高频追问

1. **请用一分钟说明并发容器与原子类的核心目标与工作机制。**

   并发容器通过分段、CAS、写时复制或无锁队列等机制提供明确的线程安全语义；原子类适合单变量或可表达为 CAS 循环的状态更新。容器方法安全不代表多个方法组合后的业务操作原子。

2. **ConcurrentHashMap的核心机制是什么？**

   读取通常不加全局锁，更新通过 CAS 与桶级同步完成；compute、merge 等方法可把单 key 的复合逻辑放进原子操作。

3. **CopyOnWrite 与队列为什么重要，实际如何落地？**

   CopyOnWriteArrayList 用写时复制换取无锁快照遍历，适合读多写极少；BlockingQueue 同时承担缓冲和生产消费协调。

4. **并发容器与原子类在工程落地时如何做取舍？**

   按访问模式选择容器，不能只看“线程安全”标签；compute 回调应短小且无外部阻塞，避免放大桶竞争；跨多个 key 或多个容器的一致性仍需锁、事务或状态机。

5. **并发容器与原子类最常见的故障与排查重点是什么？**

   在 ConcurrentHashMap 上先 get 再 put，初始化被并发执行多次；写多场景误用 CopyOnWriteArrayList，分配和复制激增；把 LongAdder.sum 当作严格事务计数。排查时应先用指标和日志确认现象，再缩小到线程、资源、依赖或数据边界。
