---
slug: java-virtual-threads
title: 虚拟线程与结构化并发
---
# 虚拟线程与结构化并发

> **先记住**：虚拟线程自 JDK 21 成为正式特性，适合大量阻塞式 I/O 的 thread-per-task 模型；它提升的是并发可伸缩性而非单任务速度。

---

## 1. 它在解决什么问题？

虚拟线程自 JDK 21 成为正式特性，适合大量阻塞式 I/O 的 thread-per-task 模型；它提升的是并发可伸缩性而非单任务速度。结构化并发在 JDK 26 仍是预览 API，应隔离使用并评估升级成本。

读这一节时，先带着三个问题：

1. **核心对象是什么？** 哪些状态会在处理过程中发生变化？
2. **主链路怎么走？** 一次处理从哪里开始，到哪里才算结束？
3. **边界在哪里？** 数据量、并发或故障出现后，哪一环最先承压？

---

## 2. 先看全貌

![虚拟线程运行时仍共享 JVM 堆](./assets/jvm_threads.png)

<p class="diagram-caption">先建立整体位置感：每个请求创建虚拟线程 → 阻塞 I/O 时卸载 carrier → 调度器运行其他虚拟线程 → 父任务统一等待、取消与收集错误</p>

### 主链路

```text
[每个请求创建虚拟线程]
    │
    ▼
[阻塞 I/O 时卸载 carrier]
    │
    ▼
[调度器运行其他虚拟线程]
    │
    ▼
[I/O 就绪后重新挂载]
    │
    ▼
[父任务统一等待、取消与收集错误]
```

先把这条链路记住即可。接下来再逐层拆开每一步为什么这样设计。

---

## 3. 核心机制，逐层拆开

### 虚拟线程调度

JVM 把大量虚拟线程挂载到少量平台线程；阻塞 I/O 时通常卸载并释放载体线程。每个任务创建虚拟线程，不应再把虚拟线程本身池化。

### 资源仍然有限

数据库连接、下游 QPS、内存和文件描述符不会因线程变轻而增加，仍需要 Semaphore、连接池和限流控制并发。

### 结构化并发

StructuredTaskScope 把相关子任务作为一个工作单元管理，统一等待、失败传播、取消和可观测性；截至 JDK 26 为第六次预览，生产应封装在适配层。

---

## 4. 用一段实现把概念落地

下面只保留最关键的主干。阅读时，把代码与上一节的流程一一对应：

```java
try (var executor = Executors.newVirtualThreadPerTaskExecutor()) {
    List<Future<Response>> futures = requests.stream()
        .map(request -> executor.submit(() -> httpClient.send(request)))
        .toList();

    for (Future<Response> future : futures) {
        consume(future.get(2, TimeUnit.SECONDS));
    }
}

// 虚拟线程降低“线程等待”成本，不会扩大数据库连接池容量。
```

### 对照着看

- **从哪里开始**：每个请求创建虚拟线程
- **关键状态变化**：调度器运行其他虚拟线程
- **怎样才算完成**：父任务统一等待、取消与收集错误

---

## 5. 怎么选才不容易错

| 关注点 | 怎么理解 | 怎么验证 |
|---|---|---|
| **正确性** | 先定义共享状态、不变量和 happens-before | 用压力测试、竞态测试和线程转储验证 |
| **吞吐** | 线程数不能突破 CPU、连接池和下游容量 | 观察排队时间而非只看完成量 |
| **延迟** | 区分锁等待、队列等待、I/O 等待和调度 | 记录 p95/p99 与超时、取消传播 |
| **故障隔离** | 任务必须有界、可取消、可拒绝 | 按依赖拆池，禁止无限队列与无限重试 |

没有脱离场景的“最佳方案”。先写清数据规模、读写比例、故障模型和延迟目标，再做选择。

---

## 6. 放进真实系统，还要考虑什么

- 阻塞式技术栈可较低成本采用虚拟线程，CPU 密集任务仍受核心数限制。
- ThreadLocal 数量和内容要审计，海量虚拟线程会放大每线程状态成本。
- 采用预览 API 时要锁定 JDK、启用预览参数并准备版本升级改造。

### 上线前检查

- 队列、等待、重试和并发度全部有界。
- 中断、取消、超时沿调用链传播，finally 释放锁与资源。
- 按依赖隔离执行器，避免一个慢下游占满全部线程。
- 压测同时观察吞吐、排队、上下文切换、锁竞争和尾延迟。

---

## 7. 常见误区与排查

### 容易踩的坑

1. 把虚拟线程接入无界请求入口，却没有限制数据库并发。
2. 长时间执行 native 或特定阻塞操作造成载体线程受限。
3. 用固定大小池管理虚拟线程，重新引入不必要的排队。

### 出现问题时，按这个顺序看

1. **还原现场**：确认受影响的请求、数据、时间窗，以及最近是否有发布或流量变化。
2. **沿链路定位**：从“每个请求创建虚拟线程”一路检查到“父任务统一等待、取消与收集错误”，找出状态第一次偏离预期的位置。
3. **验证修复**：用最小复现、压力测试或故障注入证明问题消失，同时保留回滚方案。

---

## 8. 延伸阅读

- JEP 444 Virtual Threads：https://openjdk.org/jeps/444
- JEP 525 Structured Concurrency in JDK 26：https://openjdk.org/jeps/525
- JEP 491 Virtual Thread Pinning：https://openjdk.org/jeps/491

---

## 9. 最后做一次复盘

> **一句话**：虚拟线程自 JDK 21 成为正式特性，适合大量阻塞式 I/O 的 thread-per-task 模型；它提升的是并发可伸缩性而非单任务速度。
>
> **主链路**：每个请求创建虚拟线程 → 阻塞 I/O 时卸载 carrier → 调度器运行其他虚拟线程 → I/O 就绪后重新挂载 → 父任务统一等待、取消与收集错误
>
> **关键状态**：调度器运行其他虚拟线程
>
> **最容易踩的坑**：把虚拟线程接入无界请求入口，却没有限制数据库并发。

---

## 10. 高频追问

### Q1. 请用一分钟说明虚拟线程与结构化并发的核心目标与工作机制。

虚拟线程自 JDK 21 成为正式特性，适合大量阻塞式 I/O 的 thread-per-task 模型；它提升的是并发可伸缩性而非单任务速度。结构化并发在 JDK 26 仍是预览 API，应隔离使用并评估升级成本。

### Q2. 虚拟线程调度的核心机制是什么？

JVM 把大量虚拟线程挂载到少量平台线程；阻塞 I/O 时通常卸载并释放载体线程。每个任务创建虚拟线程，不应再把虚拟线程本身池化。

### Q3. 资源仍然有限为什么重要，实际如何落地？

数据库连接、下游 QPS、内存和文件描述符不会因线程变轻而增加，仍需要 Semaphore、连接池和限流控制并发。

### Q4. 虚拟线程与结构化并发在工程落地时如何做取舍？

阻塞式技术栈可较低成本采用虚拟线程，CPU 密集任务仍受核心数限制；ThreadLocal 数量和内容要审计，海量虚拟线程会放大每线程状态成本；采用预览 API 时要锁定 JDK、启用预览参数并准备版本升级改造。

### Q5. 虚拟线程与结构化并发最常见的故障与排查重点是什么？

把虚拟线程接入无界请求入口，却没有限制数据库并发；长时间执行 native 或特定阻塞操作造成载体线程受限；用固定大小池管理虚拟线程，重新引入不必要的排队。排查时应先用指标和日志确认现象，再缩小到线程、资源、依赖或数据边界。
