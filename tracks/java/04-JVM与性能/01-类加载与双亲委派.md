---
slug: java-class-loading
title: 类加载与双亲委派
---
# 类加载与双亲委派

> **先记住**：JVM 类生命周期包括加载、验证、准备、解析、初始化和卸载。

---

## 1. 它在解决什么问题？

JVM 类生命周期包括加载、验证、准备、解析、初始化和卸载。类由“全限定名 + 定义它的 ClassLoader”共同确定身份；双亲委派优先让父加载器处理，减少核心类被重复或恶意替换。

读这一节时，先带着三个问题：

1. **核心对象是什么？** 哪些状态会在处理过程中发生变化？
2. **主链路怎么走？** 一次处理从哪里开始，到哪里才算结束？
3. **边界在哪里？** 数据量、并发或故障出现后，哪一环最先承压？

---

## 2. 先看全貌

![类加载子系统在 JVM 中的位置](./assets/jvm_architecture.png)

<p class="diagram-caption">先建立整体位置感：按类名发起加载 → 父加载器优先查找 → 读取字节码并校验 → 初始化静态代码并返回 Class</p>

### 主链路

```text
[按类名发起加载]
    │
    ▼
[父加载器优先查找]
    │
    ▼
[读取字节码并校验]
    │
    ▼
[准备静态字段并解析符号引用]
    │
    ▼
[初始化静态代码并返回 Class]
```

先把这条链路记住即可。接下来再逐层拆开每一步为什么这样设计。

---

## 3. 核心机制，逐层拆开

### 加载与初始化

加载创建 Class 对象，链接完成验证、静态字段默认值和符号解析；初始化执行类初始化方法。访问编译期常量等操作不一定触发初始化。

### ClassLoader 身份

相同字节码若由不同加载器定义，在 JVM 中也是不同类型，强转会失败。插件、容器和热部署都依赖独立加载器形成命名空间。

### 委派与破坏

父优先保护基础类并促进共享；SPI、模块容器可能使用线程上下文加载器或子优先策略，但必须限定包范围避免依赖冲突。

---

## 4. 用一段实现把概念落地

下面只保留最关键的主干。阅读时，把代码与上一节的流程一一对应：

```java
final class PluginClassLoader extends URLClassLoader {
    PluginClassLoader(URL[] urls, ClassLoader parent) {
        super(urls, parent);
    }
}

try (var loader = new PluginClassLoader(urls, Api.class.getClassLoader())) {
    Class<?> type = Class.forName(pluginClassName, true, loader);
    Plugin plugin = type.asSubclass(Plugin.class)
                        .getDeclaredConstructor()
                        .newInstance();
    plugin.start();
}
```

### 对照着看

- **从哪里开始**：按类名发起加载
- **关键状态变化**：读取字节码并校验
- **怎样才算完成**：初始化静态代码并返回 Class

---

## 5. 怎么选才不容易错

| 关注点 | 怎么理解 | 怎么验证 |
|---|---|---|
| **证据** | 先抓低开销证据，再做堆转储等重操作 | 所有结论都对应时间窗、指标和 Trace |
| **目标** | 明确优化吞吐、尾延迟、内存还是启动时间 | 一次只改一个变量并保留基线 |
| **环境** | 区分 JVM、容器限制、操作系统和下游问题 | 同时核对 cgroup、磁盘、网络与连接池 |
| **回归** | 微基准不能替代业务压测 | 用生产形态流量验证收益和副作用 |

没有脱离场景的“最佳方案”。先写清数据规模、读写比例、故障模型和延迟目标，再做选择。

---

## 6. 放进真实系统，还要考虑什么

- 插件隔离要明确 API 由父加载器共享、实现由子加载器加载。
- 热部署需确保旧 ClassLoader 及其线程、ThreadLocal、静态缓存可回收。
- 排查依赖冲突同时打印类名、加载器和代码来源。

### 上线前检查

- 事故现场先保留时间戳、版本、流量和容器资源基线。
- 采集线程转储、GC、JFR 等证据时评估额外开销。
- 调优一次只改一个变量，并保留可立即回退的配置。
- 微基准、组件压测与端到端压测分别回答不同问题。

---

## 7. 常见误区与排查

### 容易踩的坑

1. 第三方包多版本冲突导致 NoSuchMethodError。
2. 线程上下文加载器长期引用旧应用造成 Metaspace 泄漏。
3. 自定义加载器错误覆盖 java.* 或共享 API。

### 出现问题时，按这个顺序看

1. **还原现场**：确认受影响的请求、数据、时间窗，以及最近是否有发布或流量变化。
2. **沿链路定位**：从“按类名发起加载”一路检查到“初始化静态代码并返回 Class”，找出状态第一次偏离预期的位置。
3. **验证修复**：用最小复现、压力测试或故障注入证明问题消失，同时保留回滚方案。

---

## 8. 最后做一次复盘

> **一句话**：JVM 类生命周期包括加载、验证、准备、解析、初始化和卸载。
>
> **主链路**：按类名发起加载 → 父加载器优先查找 → 读取字节码并校验 → 准备静态字段并解析符号引用 → 初始化静态代码并返回 Class
>
> **关键状态**：读取字节码并校验
>
> **最容易踩的坑**：第三方包多版本冲突导致 NoSuchMethodError。

---

## 9. 高频追问

### Q1. 请用一分钟说明类加载与双亲委派的核心目标与工作机制。

JVM 类生命周期包括加载、验证、准备、解析、初始化和卸载。类由“全限定名 + 定义它的 ClassLoader”共同确定身份；双亲委派优先让父加载器处理，减少核心类被重复或恶意替换。

### Q2. 加载与初始化的核心机制是什么？

加载创建 Class 对象，链接完成验证、静态字段默认值和符号解析；初始化执行类初始化方法。访问编译期常量等操作不一定触发初始化。

### Q3. ClassLoader 身份为什么重要，实际如何落地？

相同字节码若由不同加载器定义，在 JVM 中也是不同类型，强转会失败。插件、容器和热部署都依赖独立加载器形成命名空间。

### Q4. 类加载与双亲委派在工程落地时如何做取舍？

插件隔离要明确 API 由父加载器共享、实现由子加载器加载；热部署需确保旧 ClassLoader 及其线程、ThreadLocal、静态缓存可回收；排查依赖冲突同时打印类名、加载器和代码来源。

### Q5. 类加载与双亲委派最常见的故障与排查重点是什么？

第三方包多版本冲突导致 NoSuchMethodError；线程上下文加载器长期引用旧应用造成 Metaspace 泄漏；自定义加载器错误覆盖 java.* 或共享 API。排查时应先用指标和日志确认现象，再缩小到线程、资源、依赖或数据边界。
