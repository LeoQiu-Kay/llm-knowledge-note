---
slug: java-mysql-lock-deadlock
title: MySQL 锁、幻读与死锁
---
# MySQL 锁、幻读与死锁

> **一句话定位**：InnoDB 通过记录锁、间隙锁和 next-key lock 协调当前读与写入；锁定范围取决于索引和查询条件。

---

## 1. 先给结论

InnoDB 通过记录锁、间隙锁和 next-key lock 协调当前读与写入；锁定范围取决于索引和查询条件。死锁是并发事务形成等待环，数据库会回滚受害者，应用仍需缩短事务、统一加锁顺序并安全重试。

### 面试答题路线

回答这类题不要从名词堆砌开始，按下面四步展开最稳：

1. **先定边界**：它解决什么问题，不解决什么问题。
2. **再讲机制**：把核心数据结构、状态机或调用链讲清楚。
3. **补充取舍**：说明复杂度、正确性、资源和可维护性的代价。
4. **落到生产**：给出超时、幂等、观测、压测与回滚方案。

---

## 2. 核心原理

### 锁定读与索引

UPDATE、DELETE 和 FOR UPDATE 属于当前读，会在访问的索引记录或范围上加锁；缺少合适索引可能扫描并锁住远超预期的数据。

### 间隙与幻读

可重复读下 next-key lock 把记录和前方间隙结合，限制范围内插入；快照读主要由 MVCC 提供一致视图。

### 死锁处理

InnoDB 检测等待图并选择事务回滚。死锁日志中的 SQL、索引和持锁顺序比单看报错更重要。

### 2.1 一张图建立整体心智模型

![锁通常依附于索引记录与范围](./assets/btree.png)

> **图：锁通常依附于索引记录与范围**
> （图源与许可见 [assets/CREDITS.md](./assets/CREDITS.md)）
>
> 对照本文阅读时，把图中的输入、状态、执行器和输出依次映射为：
> **事务读取或修改索引记录 → InnoDB 按访问路径加记录 / 间隙锁 → 并发事务形成等待 → 回滚代价较小事务并重试**。
> 图负责建立全局位置，具体正确性仍要回到本文的数据流、状态边界和失败路径。

---

## 3. 工作流程与关键路径

```text
[事务读取或修改索引记录]
    │
    ▼
[InnoDB 按访问路径加记录 / 间隙锁]
    │
    ▼
[并发事务形成等待]
    │
    ▼
[检测等待图中的环]
    │
    ▼
[回滚代价较小事务并重试]
```

### 3.1 每一步分别承担什么

- **入口与约束**：`事务读取或修改索引记录` 决定后续处理的语义、权限和容量边界。
- **核心决策**：`InnoDB 按访问路径加记录 / 间隙锁` 与 `并发事务形成等待` 是最容易答成“只会背名词”的地方，要讲清状态如何变化。
- **执行与副作用**：中间步骤必须说明失败是否可重试、是否会重复，以及资源何时释放。
- **完成条件**：`回滚代价较小事务并重试` 不能只看“没有抛异常”，还要有业务结果、指标或持久化证据。

### 3.2 复杂度不只是一行大 O

面试里给出时间复杂度后，还要补三件事：**数据规模是否会放大常数项、最坏路径何时触发、资源上限在哪里**。生产环境更常被队列等待、锁竞争、网络、序列化、缓存失效或外部限流拖慢；因此复杂度分析必须和实际访问分布、尾延迟及容量模型一起讲。

---

## 4. 关键实现

下面的片段不是完整框架，而是把最关键的边界写出来：

```sql
START TRANSACTION;
SELECT id, balance
  FROM account
 WHERE id IN (101, 202)
 ORDER BY id
 FOR UPDATE;

UPDATE account SET balance = balance - 100 WHERE id = 101;
UPDATE account SET balance = balance + 100 WHERE id = 202;
COMMIT;
-- 所有事务按同一顺序加锁，可显著降低死锁概率。
```

### 4.1 实现时盯住的状态

| 状态 | 需要回答的问题 |
|---|---|
| 输入 | `事务读取或修改索引记录` 是否已经校验语义、权限、大小与版本？ |
| 中间状态 | `并发事务形成等待` 能否持久化、观测或在并发下保持不变量？ |
| 副作用 | 失败重试会不会重复执行？是否有幂等键、事务或补偿？ |
| 完成 | `回滚代价较小事务并重试` 由什么证据确认？调用方能否区分成功、部分成功与失败？ |

---

## 5. 对比与选型

| 决策维度 | 面试中要说清 | 生产验证 |
|---|---|---|
| **一致性** | 明确允许的陈旧窗口、重复和丢失语义 | 设计幂等、对账与补偿而非口头承诺 exactly-once |
| **访问路径** | 从索引、分区、key 和队列说明数据怎么被定位 | 验证热点、倾斜、扫描量和回表 |
| **持久性** | 区分内存确认、日志落盘、复制完成与消费完成 | 故障注入验证崩溃和切主语义 |
| **容量** | 按峰值流量、保留期和积压估算 | 监控 lag、命中率、淘汰、磁盘和复制延迟 |

真正的选型结论应带条件：**在什么规模、读写比例、故障模型、SLO 和团队维护能力下选择它**。离开约束谈“谁更快”“谁更高级”没有工程意义。

---

## 6. 工程实践

- 统一业务资源排序后加锁，降低环路概率。
- 失败重试必须有次数、退避和幂等，不能无限重放整个请求。
- 热点行可通过分片计数、队列化或乐观并发降低竞争。

### 6.1 落地检查表

- 所有重试路径都带业务幂等键，消费者能识别重复。
- 容量模型包含峰值、保留期、积压、复制和故障切换余量。
- 用 kill、断网、磁盘满和主从切换验证持久性语义。
- 建立慢查询、命中率、lag、死信和对账差异告警。

---

## 7. 常见故障

1. 无索引更新扩大锁范围。
2. 事务内用户交互或 RPC 导致长时间持锁。
3. 捕获死锁后立即无退避重试，引发重试风暴。

### 7.1 推荐排查顺序

1. **先确认现象**：影响哪些请求、数据、租户与时间窗，是否与发布或流量变化相关。
2. **再沿关键路径定位**：从 `事务读取或修改索引记录` 逐步检查到 `回滚代价较小事务并重试`，不要跳过排队、缓存、代理或外部依赖。
3. **区分原因和结果**：高 CPU、线程多、token 多、连接满可能只是症状；用日志、指标、Trace 和状态快照互相印证。
4. **最后验证修复**：用最小复现、压力或故障注入证明问题消失，同时保留一键回滚。

最常见的错误是：**无索引更新扩大锁范围。**


---

## 8. 最简记忆

```text
一句话：InnoDB 通过记录锁、间隙锁和 next-key lock 协调当前读与写入；锁定范围取决于索引和查询条件。

主链路：事务读取或修改索引记录 → InnoDB 按访问路径加记录 / 间隙锁 → 并发事务形成等待 → 检测等待图中的环 → 回滚代价较小事务并重试
核心状态：并发事务形成等待
完成证据：回滚代价较小事务并重试
高频坑：无索引更新扩大锁范围。
```

---

## 🎯 高频追问

1. **请用一分钟说明MySQL 锁、幻读与死锁的核心目标与工作机制。**

   InnoDB 通过记录锁、间隙锁和 next-key lock 协调当前读与写入；锁定范围取决于索引和查询条件。死锁是并发事务形成等待环，数据库会回滚受害者，应用仍需缩短事务、统一加锁顺序并安全重试。

2. **锁定读与索引的核心机制是什么？**

   UPDATE、DELETE 和 FOR UPDATE 属于当前读，会在访问的索引记录或范围上加锁；缺少合适索引可能扫描并锁住远超预期的数据。

3. **间隙与幻读为什么重要，实际如何落地？**

   可重复读下 next-key lock 把记录和前方间隙结合，限制范围内插入；快照读主要由 MVCC 提供一致视图。

4. **MySQL 锁、幻读与死锁在工程落地时如何做取舍？**

   统一业务资源排序后加锁，降低环路概率；失败重试必须有次数、退避和幂等，不能无限重放整个请求；热点行可通过分片计数、队列化或乐观并发降低竞争。

5. **MySQL 锁、幻读与死锁最常见的故障与排查重点是什么？**

   无索引更新扩大锁范围；事务内用户交互或 RPC 导致长时间持锁；捕获死锁后立即无退避重试，引发重试风暴。排查时应先用指标和日志确认现象，再缩小到线程、资源、依赖或数据边界。
