---
slug: java-idempotency-retry
title: 幂等、重试与去重
---
# 幂等、重试与去重

> **先记住**：幂等保证相同业务请求重复执行不会产生额外副作用，是网络重试和至少一次投递的基础。

---

## 1. 它在解决什么问题？

幂等保证相同业务请求重复执行不会产生额外副作用，是网络重试和至少一次投递的基础。实现依赖稳定幂等键、原子状态检查与写入、结果复用，以及明确的键作用域和过期策略。

读这一节时，先带着三个问题：

1. **核心对象是什么？** 哪些状态会在处理过程中发生变化？
2. **主链路怎么走？** 一次处理从哪里开始，到哪里才算结束？
3. **边界在哪里？** 数据量、并发或故障出现后，哪一环最先承压？

---

## 2. 先看全貌

![跨服务重试必须沿调用链识别同一业务操作](./assets/otel_architecture.png)

<p class="diagram-caption">先建立整体位置感：请求携带业务幂等键 → 服务原子占位或查询历史结果 → 首次请求执行副作用 → 重复请求返回同一结果或安全拒绝</p>

### 主链路

```text
[请求携带业务幂等键]
    │
    ▼
[服务原子占位或查询历史结果]
    │
    ▼
[首次请求执行副作用]
    │
    ▼
[结果与状态持久化]
    │
    ▼
[重复请求返回同一结果或安全拒绝]
```

先把这条链路记住即可。接下来再逐层拆开每一步为什么这样设计。

---

## 3. 核心机制，逐层拆开

### 幂等键设计

键应代表同一业务意图而不是随机请求，通常包含调用方、操作类型和业务唯一号；重试必须复用同一键。

### 原子去重

数据库唯一约束、条件更新、状态机或原子缓存脚本用于把“检查并执行”合并，单独先查再写仍有竞态。

### 重试策略

只重试瞬时错误，采用指数退避、随机抖动、截止时间和最大次数；超时后先查询上一请求结果再决定重试。

---

## 4. 用一段实现把概念落地

下面只保留最关键的主干。阅读时，把代码与上一节的流程一一对应：

```sql
CREATE TABLE idempotency_record (
  tenant_id     BIGINT NOT NULL,
  idem_key      VARCHAR(128) NOT NULL,
  request_hash  CHAR(64) NOT NULL,
  status        VARCHAR(16) NOT NULL,
  response_json JSON,
  PRIMARY KEY (tenant_id, idem_key)
);

-- 原子插入成功者执行；冲突者校验 request_hash 后返回历史结果。
```

### 对照着看

- **从哪里开始**：请求携带业务幂等键
- **关键状态变化**：首次请求执行副作用
- **怎样才算完成**：重复请求返回同一结果或安全拒绝

---

## 5. 怎么选才不容易错

| 关注点 | 怎么理解 | 怎么验证 |
|---|---|---|
| **一致性边界** | 先说明哪些状态必须原子，哪些可最终一致 | 把补偿、对账和人工兜底写进方案 |
| **故障模型** | 考虑超时、重复、乱序、分区和部分成功 | 通过故障注入而非正常路径证明可靠性 |
| **容量保护** | 入口、服务和依赖分别限流与隔离 | 阈值来自压测和 SLO，不靠拍脑袋 |
| **可观测性** | 每个跨服务操作携带业务键与 trace_id | 告警能直接跳到证据和回滚动作 |

没有脱离场景的“最佳方案”。先写清数据规模、读写比例、故障模型和延迟目标，再做选择。

---

## 6. 放进真实系统，还要考虑什么

- 结果缓存提高重复请求体验，但要控制容量和保留期。
- 客户端、网关、服务和消息框架多层重试会相乘，应统一预算。
- 非幂等外部系统通过业务唯一号或对账补偿降低风险。

### 上线前检查

- 先写 SLO、故障模型和降级结果，再选中间件。
- 跨服务操作记录业务键、版本、trace_id 和补偿状态。
- 恢复策略使用退避、抖动、限次和渐进放量。
- 定期演练单实例、单机房、依赖和数据不一致故障。

---

## 7. 常见误区与排查

### 容易踩的坑

1. 每次重试生成新幂等键。
2. 仅用 Redis SETNX，键过期早于业务完成。
3. 对参数错误和永久失败持续重试。

### 出现问题时，按这个顺序看

1. **还原现场**：确认受影响的请求、数据、时间窗，以及最近是否有发布或流量变化。
2. **沿链路定位**：从“请求携带业务幂等键”一路检查到“重复请求返回同一结果或安全拒绝”，找出状态第一次偏离预期的位置。
3. **验证修复**：用最小复现、压力测试或故障注入证明问题消失，同时保留回滚方案。

---

## 8. 最后做一次复盘

> **一句话**：幂等保证相同业务请求重复执行不会产生额外副作用，是网络重试和至少一次投递的基础。
>
> **主链路**：请求携带业务幂等键 → 服务原子占位或查询历史结果 → 首次请求执行副作用 → 结果与状态持久化 → 重复请求返回同一结果或安全拒绝
>
> **关键状态**：首次请求执行副作用
>
> **最容易踩的坑**：每次重试生成新幂等键。

---

## 9. 高频追问

### Q1. 请用一分钟说明幂等、重试与去重的核心目标与工作机制。

幂等保证相同业务请求重复执行不会产生额外副作用，是网络重试和至少一次投递的基础。实现依赖稳定幂等键、原子状态检查与写入、结果复用，以及明确的键作用域和过期策略。

### Q2. 幂等键设计的核心机制是什么？

键应代表同一业务意图而不是随机请求，通常包含调用方、操作类型和业务唯一号；重试必须复用同一键。

### Q3. 原子去重为什么重要，实际如何落地？

数据库唯一约束、条件更新、状态机或原子缓存脚本用于把“检查并执行”合并，单独先查再写仍有竞态。

### Q4. 幂等、重试与去重在工程落地时如何做取舍？

结果缓存提高重复请求体验，但要控制容量和保留期；客户端、网关、服务和消息框架多层重试会相乘，应统一预算；非幂等外部系统通过业务唯一号或对账补偿降低风险。

### Q5. 幂等、重试与去重最常见的故障与排查重点是什么？

每次重试生成新幂等键；仅用 Redis SETNX，键过期早于业务完成；对参数错误和永久失败持续重试。排查时应先用指标和日志确认现象，再缩小到线程、资源、依赖或数据边界。
