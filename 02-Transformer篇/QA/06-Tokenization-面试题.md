# Tokenization · 面试题

> 对应原理文档：[06-Tokenization.md](../06-Tokenization.md)
> 标注说明：难度 ⭐(简单)→⭐⭐⭐⭐(难)；高频 🔥(偶尔)→🔥🔥🔥(必问)

---

## Q1: 为什么 LLM 用 subword 而不是字符级或词级？
> 难度 ⭐⭐ ｜ 高频 🔥🔥

| 粒度 | 优点 | 缺点 |
|---|---|---|
| 字符级 | 词表小（ASCII 几十个） | 序列太长（"hello" → 5 token），模型需自学组词 |
| 词级 | 语义直观 | 词表巨大（百万级），**OOV** 严重，新词无法表示 |
| **Subword** | 词表中等（30K-150K）、几乎无 OOV、高频整词低频拆分 | 数学/代码处理需调优 |

**OOV**（Out-Of-Vocabulary）：训练词表里没见过的词。词级 tokenizer 遇到新词只能用 `<UNK>` 替代，丢失信息。

Subword 是**复杂度和表达力的甜点**，LLM 时代的事实标准。

---

## Q2: BPE 算法的步骤是什么？举个例子
> 难度 ⭐⭐ ｜ 高频 🔥🔥🔥

**BPE**（Byte Pair Encoding，字节对编码）：原本是压缩算法（Sennrich et al. 2016 引入 NMT）。

**算法**：
1. 初始化：词表 = 所有字符
2. 在训练语料上统计**相邻 token 对**的频率
3. 选频率最高的对（如 `("l", "o")`），合并成新 token（`"lo"`），加入词表
4. 在所有出现位置替换为新 token
5. 重复直到词表达到目标大小

**示例**：

```
初始:        l o w </w>   l o w </w>   n e w e s t </w>   w i d e s t </w>
合并 (l,o):  lo w </w>    lo w </w>    n e w e s t </w>   w i d e s t </w>
合并 (lo,w): low </w>     low </w>     ...
合并 (e,s):  low </w>     low </w>     n e w es t </w>    w i d es t </w>
...
```

**关键直觉**：高频片段被合并成整 token（"low" 整体），低频组合仍保持拆分状态。

---

## Q3: Byte-level BPE 和普通 BPE 的区别？为什么 GPT-2 起广泛使用？
> 难度 ⭐⭐ ｜ 高频 🔥🔥

**普通 BPE**：底层是 Unicode 字符（数十万种）。
- 训练语料未见过的字符（生僻 emoji、罕见汉字）需要 `<UNK>` 兜底
- 词表初始化集合受限

**Byte-level BPE**（GPT-2 / LLaMA-3 / Qwen）：底层是 **256 个字节**。
- 任何 Unicode 字符都能拆成 1-4 个 UTF-8 字节
- **真正 0 OOV**：从未见过的字符也能用字节序列表示
- 代价：罕见字符 token 数量多（如一个生僻汉字拆 3-4 token）

**为什么 GPT-2 选 byte-level**：保证对**任意输入**都能编码（包括奇怪 Unicode、控制字符、二进制片段），鲁棒性远超字符级 BPE。后续 LLaMA-3、Qwen 全部跟进。

---

## Q4: WordPiece 和 BPE 的区别是什么？
> 难度 ⭐⭐ ｜ 高频 🔥

**核心算法相似**：都是迭代合并最优的相邻对。**关键差异：判据不同**。

| | 判据 | 含义 |
|---|---|---|
| BPE | 频率最高的对 | 共现次数最多 |
| WordPiece | $\arg\max \dfrac{P(AB)}{P(A) P(B)}$ | **点互信息（PMI）** |

**WordPiece 直觉**：不仅看 $A$ 和 $B$ 出现得多，还要求 $A$ 和 $B$ **一起出现的概率明显高于独立出现**——即真正的"搭配"。

**代表**：BERT、DistilBERT 用 WordPiece。

**实际差异**：两者效果接近，主流 LLM（GPT、LLaMA、Qwen）选了 BPE/SentencePiece-BPE，WordPiece 渐少。

---

## Q5: SentencePiece 是什么？它解决了什么问题？
> 难度 ⭐⭐ ｜ 高频 🔥🔥

**SentencePiece**（Google 的库）支持多种算法（BPE、Unigram LM），核心三个特点：

**1. 不依赖空格分词**
- 把空格视为普通字符（用 `▁` 表示）
- 适合中文、日文、泰文等**无空格语言**

**2. 可逆**
- 原文 → tokens → 原文 不丢失信息（包括空格位置）
- 对多语种、代码等关键

**3. 支持 Unigram LM 算法**（与 BPE 方向相反）：
1. 初始化**大词表**（如 100 万候选 subword）
2. 用 EM 算法估计每个 subword 的概率
3. 迭代**删掉**对似然贡献最小的 subword
4. 直到词表达到目标大小

**用户**：T5、ALBERT、LLaMA、Qwen 都用 SentencePiece。

**LLaMA 用的就是 SentencePiece-BPE**：BPE 算法 + SentencePiece 框架（不依赖空格）。

---

## Q6: Tokenizer 训练完能改吗？为什么"难"？
> 难度 ⭐⭐⭐ ｜ 高频 🔥🔥

**结论**：基本**不能改**，预训练后改 tokenizer 几乎要重训。

**原因**：
1. **Embedding 对齐**：每个 token id 对应 embedding 矩阵的一行。修改 tokenizer 后 id ↔ embedding 的映射全乱
2. **合并顺序敏感**：BPE 推理时严格按训练时的合并顺序应用，改一条合并规则会引发连锁
3. **lm_head 同步**：输出层也需扩容/重映射

**有限的"嫁接"方法**：
- **扩展词表**（如加中文 token）：保留原 embedding，新 token 用平均初始化或 mean pooling，然后**少量预训练**对齐
- **特殊 token**（`<|im_start|>` 等）：可加，但只是"插入"不是修改
- **完全替换**：等于重训

**给面试官的总结**：
```
Tokenizer 在模型生命周期里是"凝固"的——
选它时就要对齐目标语料和应用场景。
```

---

## Q7: 词表大小怎么选？大小词表的取舍是什么？
> 难度 ⭐⭐⭐ ｜ 高频 🔥🔥

| 词表大（200K） | 词表小（32K） |
|---|---|
| ✅ token 数少（同文本短 → 推理快、context 用得省） | ❌ 序列长（同文本拆得碎） |
| ❌ embedding + lm_head 参数巨大 | ✅ embedding 层小 |
| ❌ 长尾 token 训练不充分 | ✅ token 训练充分 |

**典型选择**：

| 模型 | 词表 | 备注 |
|---|---|---|
| GPT-2 | 50K | 英文为主 |
| LLaMA-1/2 | 32K | 偏英文，**对中文不友好**（一个汉字常拆 3-4 byte） |
| **LLaMA-3** | **128K** | 大幅扩词表 |
| Qwen-2 | 152K | 多语种友好 |
| GLM-4 | 152K | 中文优化 |

**经验**：
- 多语种 / 中文场景至少 100K+
- 词表大小的边际收益在 100K-150K 后递减
- embedding 层参数 = $\|V\| \times d$，大词表显著推高小模型的参数占比

---

## Q8: 中文场景下 LLaMA-2 为什么 token 经济性差？怎么改善？
> 难度 ⭐⭐⭐ ｜ 高频 🔥🔥

**LLaMA-2 词表 32K，绝大部分是英文 BPE 合并结果**：
- 中文字符大多没有专门 token
- 一个汉字常被拆成 **2-4 个 UTF-8 字节 token**

**后果**：
- 同样意思的中文比英文 token 数多 2-4 倍
- 推理慢、context 利用率低
- 中文场景下 LLaMA-2 的"实际可用上下文"被压缩

**改善方法**：

| 方案 | 做法 | 代价 |
|---|---|---|
| **扩词表 + 继续预训练** | 加几千常用汉字 token，少量 step 继续训 | 改动 embedding，需要少量算力 |
| **换 tokenizer 重训** | 用中文友好的 tokenizer（如 LLaMA-3 / Qwen） | 巨大算力 |
| **直接用中文模型** | Qwen / DeepSeek / GLM | 切换模型 |

**LLaMA-3 的解法**：把词表扩到 128K，中文 token 经济性显著改善。Qwen-2 则用 152K 中文友好词表，对中文 0.5-0.6 token / 汉字（vs LLaMA-2 的 3-4）。

---

## Q9: Tokenizer 怎么处理数字？为什么数字处理不好会影响数学能力？
> 难度 ⭐⭐⭐ ｜ 高频 🔥

**早期 tokenizer 的乱拆问题**：
- GPT-2 把 `12345` 拆成 `[123, 45]` 或 `[1, 234, 5]`——取决于合并规则
- 同一个数字在不同上下文被拆成不同 token 序列
- 模型无法稳定学到"位值"概念

**LLaMA-3 / GPT-4 的解法：per-digit 切分**
- 把每个数字字符强制单独成 token：`12345` → `["1", "2", "3", "4", "5"]`
- 数字始终拆成单数字序列，模型能稳定学到 base-10 位值

**为什么提升数学能力**：
- 一致表示 → 模型能泛化加减乘除规则
- 不再受合并顺序的随机性干扰
- 实测对算术任务、长数字推理有显著提升

**追问：BPE 推理时为什么必须严格按训练顺序应用？** 合并顺序决定 token 化结果。乱序会产生不同的 token 序列，与训练时的分布不一致，相当于喂给模型陌生输入。

---

## 🎯 自测清单

- [ ] 能讲清字符级 / 词级 / subword 三层取舍
- [ ] 能描述 BPE 的迭代合并步骤（含一个例子）
- [ ] 能区分 byte-level BPE 和普通 BPE（256 字节底层 → 0 OOV）
- [ ] 能讲 BPE / WordPiece / Unigram LM 三种算法的判据差异
- [ ] 能解释为什么 tokenizer 训完几乎不能改（embedding 对齐）
- [ ] 能讲 LLaMA-2 中文经济性差的原因 + LLaMA-3 / Qwen 的改善
- [ ] 能讲 per-digit 切分对数学能力的影响
